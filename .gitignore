# OS
.DS_Store
Thumbs.db

# Coding-assistant local settings (machine-specific, never shared)
.claude/settings.local.json

# Local pointer/mirror for tooling that expects a root-level CLAUDE.md;
# the tracked project log lives at docs/PROJECT-LOG.md (see that file).
/CLAUDE.md

# Node
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# Next.js
apps/web/.next/
apps/web/out/

# Build output
dist/
build/
*.tsbuildinfo

# Testing
coverage/
playwright-report/
test-results/

# Editors
.vscode/*
!.vscode/extensions.json
.idea/

# CLI-local project links (machine-specific, not app config)
.vercel/
supabase/.temp/

# Local reference project clone — architectural ideas only, never copied
# code, kept only for local reference and never committed. See
# docs/PROJECT-LOG.md design decisions for the license/attribution rationale.
external-reference/

# Personal reference material unrelated to this project — never committed.
/Rollout Plan/

# Local-only research material and its archive — never committed, never
# uploaded to GitHub.
/research/
/research.zip
/palimnote_phases_19_23_plan_revised.md
/palimnote_roadmap_visualizer_and_competency_plan.md

# tesseract.js downloads its OCR language model into the worker's CWD at
# runtime the first time OCR is exercised — a cache artifact, not source.
apps/worker/eng.traineddata

# Local-only gold-baseline fixtures (copyrighted source PDFs must never enter git)
baseline-test/

# One-off prepared production-migration SQL (contains the drizzle ledger
# bookkeeping insert for a specific deploy; applied by hand via
# `supabase db query --linked`, never committed — see docs/PROJECT-LOG.md's
# migration-application recipe).
/prod-migration-*.sql
