/**
 * Barrel export (spec §1.1's `index.ts` row) — `KnowledgeMapWorkspace` is
 * the only thing a route `page.tsx` file should import from this
 * directory.
 */
export { KnowledgeMapWorkspace, type KnowledgeMapWorkspaceProps } from "./KnowledgeMapWorkspace";
