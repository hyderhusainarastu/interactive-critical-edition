# Completed Tasks Archive — Interactive Critical Edition

Full per-phase completed-task detail, moved out of `docs/PROJECT-LOG.md` on 2026-07-20 to keep that file within a manageable size for every session's context. This is a historical record — nothing here is load-bearing for resuming work; `docs/PROJECT-LOG.md`'s condensed "Completed Tasks" section plus "Current Implementation Status" are the authoritative current-state summary. Phase 9 sub-phases stay in `docs/PROJECT-LOG.md` directly since that work is still active.

## Completed Tasks

**Phase 0:**
- [x] Full implementation plan written and approved (`docs/architecture/plan.md`).
- [x] Reference project inspected via GitHub API; license verdict recorded (no LICENSE file found).
- [x] Filesystem case-sensitivity constraint on the project log's filename verified and documented.
- [x] `git init`, `.gitignore`, `README.md`, `docs/PROJECT-LOG.md`, `.env.example` created and committed (`0b148b6`).
- [x] Private GitHub repo created (`gh repo create --private --source=. --push`), initial commit pushed to `main`.
- [x] `phase-0-complete` tag created and pushed.

**Phase 1a (local foundation):**
- [x] Toolchain bootstrapped on this machine: Node 24 LTS, pnpm via Corepack, Colima + Docker CLI + `docker-compose` plugin (see Known Problems for setup gotchas).
- [x] Repo-local git identity set (`git config --local`, not `--global`).
- [x] pnpm monorepo scaffold: root `package.json`/`pnpm-workspace.yaml`, `apps/web` (Next.js App Router + Tailwind v4 + warm-palette design tokens per plan §19), `packages/db` (Drizzle ORM).
- [x] Local Postgres + pgvector via `docker-compose.yml` (Colima runtime).
- [x] Drizzle schema (Phase 1 scope: `user`/`account`/`session`/`verification_token`/`password_reset_token`) + 2 migrations applied locally.
- [x] Auth.js v5 wired: Credentials provider, bcrypt hashing, `DrizzleAdapter`, JWT sessions + `sessionVersion` revocation (see Design Decisions).
- [x] Signup, email verification, login, password reset flows — pages + server actions + API routes, all tested live against the local dev server (not just typechecked).
- [x] `MailProvider` adapter: `ResendMailProvider` / `ConsoleMailProvider` fallback (verified the console fallback logs a working link when `RESEND_API_KEY` is unset).
- [x] Protected `/dashboard` page (server-side `auth()` check, verified redirects unauthenticated requests to `/login`).
- [x] GitHub Actions CI (`.github/workflows/ci.yml`): lint, typecheck, test, build, against an ephemeral Postgres service container — no external secrets required.
- [x] `phase-1a-complete` tag.

**Phase 1b (real Supabase + Vercel):**
- [x] Supabase org + project created via CLI (personal access token), `pgvector` enabled, Phase 1 migrations applied and verified against the real DB.
- [x] Vercel project created and linked; **Root Directory set to `apps/web`** via the API (see Known Problems — this is required for a pnpm monorepo, `vercel link`/`vercel --prod` from the subdirectory alone silently uploads only that subtree and loses pnpm-workspace context).
- [x] Production env vars set (`DATABASE_URL` = Supabase transaction pooler :6543, `DIRECT_URL` = Supabase direct :5432, `AUTH_SECRET` = freshly generated, distinct from the local dev one, `AUTH_URL`/`NEXT_PUBLIC_APP_URL` = the assigned `*.vercel.app` domain).
- [x] Deployed to production; full auth flow (signup/verify/login/dashboard) re-verified live against https://interactive-critical-edition.vercel.app and the real Supabase DB.
- [x] Test user cleaned up from the production DB after verification.
- [x] `phase-1-complete` tag.

**Phase 2a (Upload and Library, local):**
- [x] Schema: `work`, `edition`, `document`, `processing_job` tables (simplified subset of plan §9 — no shared canonical-work catalog or separate `authors` table yet, see Design Decisions), migration `0002_colossal_zarda.sql`.
- [x] Supabase Storage bucket `documents` created (private, 50MB limit, PDF/TXT/Markdown allowlist).
- [x] `packages/ingestion` created: shared Storage access (`uploadDocumentFile`/`downloadDocumentFile`/`deleteDocumentFile`) + parsers (`parsePdf` via `unpdf`, `parseText` for TXT/Markdown) dispatched by `parseDocument(buffer, mimeType)`.
- [x] `apps/worker` created: pg-boss consumer for the `extract-text` queue, runs the parser, updates `document.processing_status`/`extracted_text`/`extracted_title`/`extracted_author`, tracks attempts/errors in `processing_job`.
- [x] `(app)` route group with a centralized `requireSession()`/`getApiUserId()` auth check (see Known Problems for why these exist instead of raw `auth()` calls) — dashboard, upload, and work-detail pages moved under it.
- [x] Upload flow: dropzone UI (`/upload`) → signed direct Supabase Storage PUT (the browser never sends file bytes through Vercel) → authenticated init/complete routes. It enforces the MIME allowlist, 50MB file cap, 500MB per-user quota, atomically queues extraction, and validates content plus the optional ClamAV seam in the worker before parsing. This avoids serverless request-body limits while keeping uploaded documents private.
- [x] Work detail page (`/works/[workId]`): polls processing status, shows a metadata confirm form once `needs_review`, shows the ready/failed state otherwise; `GET/POST /api/works/[workId]/status` and `/confirm` routes, both 404 (not 403) on another user's work id.
- [x] Dashboard rebuilt as a library listing (work title/author/status, links to each work's page).
- [x] Full flow verified live: register → verify → login → upload (.txt and a hand-crafted text-layer .pdf) → worker extracts text and detects title → confirm metadata → appears in library as "Ready". Also verified: cross-user 404 on both the API and the page route, and all four upload-security rejections (wrong MIME, mislabeled content, empty file, unauthenticated).
- [x] Test data (DB rows and the actual Storage files) cleaned up after verification.
- [x] Migration `0002` also applied to the **production** Supabase DB (the web app is already live — leaving it without the Phase 2 tables would have broken the production upload route the moment it's used, even before Phase 2b's worker exists to consume the queue).
- [x] `phase-2a-complete` tag.

**Phase 2b (Render worker deployment):**
- [x] Render CLI installed; `render login`'s browser flow doesn't work in this sandbox (no TTY, same as Supabase/Vercel) — used a personal access token (`RENDER_API_KEY` env var) instead, which the CLI supports natively for non-interactive use.
- [x] GitHub connected to Render (dashboard OAuth step, done by the user — Render can't fetch even its own now-authorized repos without this, distinct from Vercel's model).
- [x] Payment method added (dashboard, done by the user — Render's free tier doesn't support persistent background workers, confirmed exactly as the plan predicted; a card is required before any paid-plan service can be created at all, not just at billing time).
- [x] Background worker service created via `render services create` (`srv-d9dgiamrnols73ccpa10`, `starter` plan, `virginia` region, Node runtime, build `corepack enable && pnpm install --frozen-lockfile`, start `pnpm --filter worker start`), auto-deploy from `main` enabled.
- [x] Production env vars set via the Render REST API (`PUT /v1/services/:id/env-vars` — the CLI's `services update` has no env-var flag): `DATABASE_URL` (Supabase pooler), `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_STORAGE_BUCKET`.
- [x] First deploy crashed (`tsx --env-file=.env` errors when `.env` doesn't exist, and it never will on Render — env vars come from the platform directly). Fixed by switching both `dev` and `start` scripts to `tsx --env-file-if-exists=.env`, tested locally both with and without the file present, then pushed (auto-deploy picked it up).
- [x] Full production pipeline verified live end-to-end: registered/verified/logged in against the production web app, uploaded a `.txt` file, confirmed via Render's logs that the **Render worker** (not a local process) picked up the job and extracted the text, confirmed the metadata through the production API, and saw the correct `needs_review` → confirmed state. This is the whole real deployed system working together, not a partial/local-only check.
- [x] Test data (production DB user — cascades to work/document/processing_job — and the Storage file) cleaned up after verification.
- [x] `phase-2-complete` tag.

**Phase 3 (Reader):**
- [x] Schema: `documents.last_position` (jsonb) added; new `footnote`, `highlight`, `note`, `bookmark` tables. Migration `0003_broad_blade.sql`, applied to both local and production Postgres.
- [x] Heuristic footnote detector (`packages/ingestion/src/parsers/footnotes.ts`): finds a trailing block of consecutive numbered lines and keeps only entries whose number also appears as an in-body `[N]`/`(N)` marker earlier in the text — the in-body cross-check is what actually guards against false positives (e.g. a table of contents), not the run length. Wired into `apps/worker` for `text/plain`/`text/markdown` documents only (see Remaining Tasks re: PDF).
- [x] `getSignedDocumentUrl()` added to `packages/ingestion/src/storage.ts` for client-side PDF fetching (signed, time-limited, never a public bucket URL).
- [x] Full reader feature under `apps/web/src/app/(app)/works/[workId]/reader/`: quote-anchored highlight system (`highlightDom.ts` — text-fingerprint via quote+prefix+suffix context, not raw coordinates, so highlights survive re-render/reflow — plan §25 risk R3), `TextReader.tsx` (paragraph reader, footnote markers, IntersectionObserver reading-position tracking, selection-to-highlight UI), `PdfReader.tsx` (pdfjs-dist canvas+text-layer rendering, page nav, highlight application to the text layer), `ReaderShell.tsx` (orchestrator: theme/font-size/line-width/distraction-reduced-mode state, dispatches Pdf/TextReader, split-pane via a recursive embedded `ReaderShell`), `NotesSidebar.tsx`, `WorkPicker.tsx` (split-pane work selector).
- [x] Full reader REST API under `apps/web/src/app/api/works/[workId]/reader/`: main GET (document + footnotes + highlights + notes + bookmarks + last position + signed PDF URL), `position` (POST), `highlights`/`notes`/`bookmarks` CRUD (list + per-id delete), all via `getApiUserId()` + `getOwnedDocument()` (404, not 403, on another user's work — consistent IDOR posture with Phase 2). New `GET /api/works` listing the user's ready works, for the split-pane picker.
- [x] `apps/web/src/lib/auth.ts` centralized further with `requireSession()`/`getApiUserId()` used throughout the new reader routes (see Known Problems for why these exist instead of relying on type-augmented `auth()` calls directly).
- [x] pdf.js worker script vendored as a static file (`apps/web/public/pdf.worker.min.mjs`) instead of resolved via `import.meta.url`, for reliability under Turbopack; excluded from ESLint via `globalIgnores` (see Known Problems for the re-copy-on-upgrade maintenance note).
- [x] Playwright installed and configured (`apps/web/playwright.config.ts`, runs against an already-running local stack — not CI-wired yet, see Remaining Tasks). Two full E2E specs (`apps/web/e2e/reader.spec.ts`), both passing: (1) upload → open reader → click footnote marker → select text → highlight → add note → add bookmark → scroll → reload → confirm reading position and highlight both survive the reload (a real re-render, not just in-memory state); (2) split-pane opens a second work alongside the first and closes cleanly.
- [x] `apps/web/e2e/helpers.ts`: `createVerifiedTestUser`/`deleteTestUser` fixtures; `deleteTestUser` purges the user's Supabase Storage files (not just DB rows — the DB cascade has no knowledge Storage objects exist) after discovering 16 orphaned files from earlier manual + first-run test uploads, which were swept and deleted once confirmed no real users existed in either DB.
- [x] Bug fixed in the initial footnote heuristic: a single-footnote document (`Notes\n\n1. ...`) was silently detected as zero footnotes because the original code required a run of 2+ consecutive numbered lines. Removed that requirement (the in-body-marker cross-check already prevents false positives on its own); verified via a manual `tsx` script against three cases (single-footnote now detected, multi-footnote still detected, false-positive table-of-contents still correctly empty) plus the full Playwright suite re-passing. Committed (`ae9cfd6`), pushed, CI confirmed green (run `29633299747`), Render worker redeployed and confirmed live.
- [x] Test data (DB rows and Storage files, both local and production) cleaned up after every verification pass.
- [x] `phase-3-complete` tag.

**Phase 4 (Scholarly Analysis):**
- [x] Schema: `bibliographic_record`, `citation`, `annotation`, `graph_edge`, `ai_usage_log` tables; the 10-category `relationship_category` enum; `verification_status`/`provenance_source`/`access_status`/`edge_type`/`analysis_status` enums; `document.analysis_status`/`analysis_error`. Migration `0004_real_anthem.sql`, applied to local and production.
- [x] `packages/ai-adapters`: `LLMProvider` interface + fetch-based `OpenAIProvider`/`AnthropicProvider` (no SDK dep), cost-first task routing with env-overridable model IDs and per-call cost estimation, deterministic `heuristicClassify` fallback, `classifyRelationship()` entrypoint (real model when a key is set, else heuristic — provenance always honest). `CLASSIFY_PROMPT_VERSION` stored on every annotation.
- [x] `packages/bibliographic`: keyless `CrossrefSource`/`OpenAlexSource`/`OpenLibrarySource` behind one `BibliographicSource` interface; `resolveCitation()` tries them in order with a title-overlap confidence guard; unmatched citations stay unresolved (never guessed).
- [x] `packages/ingestion`: heuristic `extractCitations()` (reference-section entries + inline author–year), the cheap first stage of the two-stage pipeline.
- [x] `apps/worker`: `analyze-work` queue + `analyzeWork()` pipeline — extract citations → resolve bibliographically → anchor (text docs) → classify → write `annotations`/`citations`/`graph_edges`/`ai_usage_logs` with full provenance; idempotent re-runs preserve user edits.
- [x] Analysis enqueued automatically on metadata-confirm (work → ready); manual re-trigger via `POST /api/works/[workId]/analyze`.
- [x] Reader API: main GET now returns annotations + analysis status; `GET .../reader/annotations` (polling), `PATCH .../reader/annotations/[id]` (approve/dispute/reject/hide/edit) — all IDOR-safe (404 not 403).
- [x] Reader UI: `AnnotationsPanel` (category glyph+label+color, target + access status, always-visible confidence, honest provenance, verbatim source passage, correction workflow, 10-category legend, non-dismissible AI-research-aid disclaimer, analysis-status badge with live polling, re-analyze); inline category-colored superscript markers in `TextReader` (single-point insertion coexisting with the highlight layer) that open the corresponding annotation.
- [x] Vitest added (root `test` script was a no-op until now); 23 unit tests across the three new packages, all passing.
- [x] Playwright E2E (`apps/web/e2e/annotations.spec.ts`) covering upload → confirm → analysis → category-coded annotations → approve → reload-persistence. Fixed a real ordering bug (confidence-tie instability) surfaced by it; raised the suite's test timeout/retries for the live-API load (see Known Problems).
- [x] Fixed the worker `dev` script arg order (`tsx watch` must precede flags on tsx 4.23.1); `start` was unaffected so production was never impacted.
- [x] Verified through the deployed system: migration applied to prod via the Supabase Management API (Vercel stores the DB URLs as Sensitive, so `vercel env pull` couldn't provide them — see Known Problems); web deployed to Vercel, worker auto-deployed to Render; seeded a ready doc in prod, logged in on the live site, triggered `/analyze`, confirmed the production worker resolved citations against live Crossref and wrote 2 annotations (`analysis_status: complete`); all prod test data cleaned up.
- [x] `phase-4-complete` tag.

**Phase 5 (Reading Roadmap, Knowledge Profile, 3D Graph):**
- [x] Schema: `reading_records`, `understanding_ratings`, `roadmap_overrides` + `reading_status`/`priority_tier` enums. Migration `0005_lazy_black_knight.sql`, applied local and production. (No `reading_roadmaps`/`roadmap_items` snapshot tables — roadmap is computed on demand, see Design Decisions.)
- [x] `packages/roadmap`: pure `rankRoadmap()` — category→tier mapping, graph-centrality tiebreak, known/completed deprioritization ("review only"), concise/comprehensive mode, beginner/intermediate/advanced expertise filter, greedy time-budget pass, manual overrides (hide/tier-pin/position-pin). 14 unit tests incl. the Heidegger and Vico acceptance cases (plan §13 step 9).
- [x] `apps/web/src/lib/roadmap.ts`: recursive-CTE traversal over `graph_edges` (transitive through normalized-title-matched owned works) feeding `rankRoadmap`; verified against the real DB.
- [x] `apps/web/src/lib/graph.ts`: per-user knowledge-graph builder (works + referenced readings, with read/reading/unread/missing state) for both the 3D view and the table.
- [x] API: `GET /works/[workId]/roadmap` (compute, with mode/expertise/maxMinutes params), `POST /works/[workId]/roadmap/item` (upsert understanding rating / reading status / override), `GET /works/[workId]/graph` (work-scoped) and `GET /api/graph` (global). All IDOR-safe.
- [x] Roadmap UI (`RoadmapView`): tier-grouped ranked sequence, per-item understanding slider + reading-status + hide, depth/level/time-budget filters that recompute. Linked from the work page.
- [x] Graph UI (`components/graph/*`): `GraphView` orchestrator with a 3D⇄table toggle (table default), `KnowledgeGraph3D` (react-force-graph-3d, dynamic ssr:false, restrained), `GraphAccessibleFallback` (sortable/filterable/keyboard+SR table — plan §20), legend, stats, node detail. Work-scoped page + global `/graph` page. Linked from dashboard + work page.
- [x] Vitest total now 37 (12 ai-adapters + 6 bibliographic + 5 ingestion + 14 roadmap); typecheck/lint/build clean.
- [x] Playwright `roadmap.spec.ts` through the full stack, passing. Fixed a real bug it surfaced: drizzle expands a JS array to a param list `($1,$2)`, valid for `IN` but not `= ANY(...)` — both raw queries switched to `IN` (had been silently returning empty roadmaps/graphs). E2E suite serialized (`workers: 1`) to stop parallel spec files piling analysis jobs on the one shared worker.
- [x] Deployed: migration `0005` applied to prod via the Supabase Management API; web deployed to Vercel; new routes verified live and auth-gated. `phase-5-complete` tag.

**Phase 6 (Landing Page & Onboarding):**
- [x] Public landing page (`app/page.tsx`): warm scholarly design (serif display + Geist + the existing palette) — hero, three capability showcases (annotated reader / roadmap / knowledge graph) with **restrained static** illustrations (a passage+annotation card, tiered roadmap, hand-authored SVG graph sketch — deliberately NOT the WebGL 3D, per plan §19), how-it-works, researcher-vs-newcomer, a reliability/privacy section, and CTAs. Shared `SiteHeader` (adapts to signed-in state) + `SiteFooter`.
- [x] Policy pages: `/privacy` (data storage, isolation, copyright/BYO-texts, AI handling, deletion/export, research-aid disclaimer) and `/terms` — plain-language, plan §15.
- [x] Onboarding: `users.preferences` jsonb (migration `0006`); a skippable `/welcome` step capturing expertise level (seeds the roadmap's default) with a first-upload nudge; `completeOnboardingAction` stamps `onboardedAt`; the dashboard routes new users through `/welcome` until then; the roadmap page defaults its level to the stored expertise.
- [x] `@axe-core/playwright` added; `landing.spec.ts` (content + WCAG 2A/2AA scan, zero violations) and `onboarding.spec.ts` (first-login routing → complete → library) both passing. Landing verified visually via screenshot.
- [x] Deployed: migration `0006` applied to prod (Management API); web deployed to Vercel; live landing/policy/welcome routes verified. `phase-6-complete` tag.

**Phase 7 (Hardening & Deployment):**
- [x] Security — authorization-bypass matrix (`e2e/security.spec.ts`, plan §21/§25 R5): an authenticated attacker gets **404 on all 16** read/write routes for another user's work, and **401** on 5 unauthenticated. Added `seedOwnedWork()` fixture. Prompt-injection hardening in `classify.ts` (fenced + triple-quote-stripped source text, enum-constrained output) with a test proving an injection payload can't drive the classifier. Upload-security checks were already in place from Phase 2.
- [x] AI-reliability eval harness (`ai-adapters/eval.test.ts`, plan §21): gold-standard citation→category cases with an accuracy gate on the heuristic baseline — the promotion gate for when a real model is wired.
- [x] Admin dashboard (`/admin`, plan §20): platform counts, the AI usage/cost view over `ai_usage_logs`, processing-job health + recent failures. Env-allowlist gating (`ADMIN_EMAILS`) with a 404 for non-admins; admin nav link shown only to admins. `ADMIN_EMAILS` set in prod to the owner's email.
- [x] Error-reporting seam: `@ice/observability` `reportError()` (Sentry-ready, structured local fallback), wired into the worker's extract/analyze failures and the web upload route.
- [x] Performance: 15 indexes (migration `0007`) on the hot filter/join columns (graph_edges by user+source / user+target, per-document reader + annotation tables, work/document by owner, Phase 5 profile tables). Applied local + prod.
- [x] CI now runs the CI-safe E2E specs (landing/onboarding a11y + the authorization matrix — web + Postgres, no worker/live APIs) after build; verified green in CI. Analysis/reader/roadmap specs stay manual (documented).
- [x] Recovery drill: materialized the `phase-5-complete` tag in a git worktree and confirmed it installs (`--frozen-lockfile`) and typechecks — a genuine restore point (plan §23 "restore from an earlier tagged checkpoint").
- [x] README overhaul (feature overview, built stack + package list, test/E2E workflow); docs/PROJECT-LOG.md brought current.
- [x] Deployed: migration `0007` applied to prod; web + worker deployed; `/admin` live and auth-gated. `phase-7-complete` tag.
- [ ] **Honest gaps (need real infra / manual effort, documented not silently skipped):** a *manual* screen-reader (VoiceOver) pass — only the automated axe scan (zero violations) was run; a k6 load test — indexes landed as the perf improvement but no load run; real Sentry — the seam is wired but no DSN is provisioned (structured local logging until then); a live DB backup/restore drill — Supabase's managed backups cover this and the git-checkpoint recovery was done, but a point-in-time DB restore wasn't exercised.

**Post-Phase-7 operational verification:**
- [x] Restored and verified authenticated CLI access for GitHub (`gh`, including repo/workflow scopes), Supabase, Vercel, and Render. Credentials remain in the tools' own secure stores/Keychain, never the repository.
- [x] Re-ran the implemented product surface: all 40 Vitest tests, all 9 Playwright tests, typecheck, and the production build passed. Real OpenAI (`gpt-4o-mini`) and live bibliographic API calls succeeded.
- [x] Re-tested the deployed system with a dedicated verified non-admin acceptance account: browser login succeeded; a real PDF upload reached `needs_review` through the production Render worker in about four seconds with no processing error. The account/data remain only for the user's current manual acceptance pass and need explicit cleanup afterward.
- [x] Audited the production integrations without exposing values: migration ledger is current through `0007`; required Render/Vercel environment-variable names are configured; worker and web deployments are live; local and GitHub `main` matched before this documentation update.
- [x] Found one reproducible auth UX defect during negative-path testing: an invalid password lets `InvalidCredentialsError` escape from `apps/web/src/lib/auth.ts` through `apps/web/src/lib/actions.ts`, producing an error response/runtime failure instead of returning the login form's friendly “Invalid email or password” state. Correct credentials still work locally and in production. Tracked first in Remaining Tasks below; not silently treated as passing.


## Phase 9 (Interactive Learning Workspace) — sub-phase detail

Moved from `docs/PROJECT-LOG.md`'s "Completed Tasks" section on 2026-07-22 (that file exceeded a 150,000-character tooling limit). Full sub-phase-by-sub-phase detail below; `docs/PROJECT-LOG.md` keeps a condensed one-line-per-sub-phase summary plus the tag `phase-9-complete`. This narrative is also independently preserved in full in `docs/CHANGELOG.md`.

- [x] **9.1 — Foundations & flag (2026-07-20).** `@ice/config` parses `ANALYSIS_PIPELINE` into an ordered `v1|v2|v3` (`isEditionPipeline()` replaces exact-equality checks). Migration `0015`: four-level `reader_level` enum + backfill from `preferences.expertise`, `concept`/`concept_mastery`/`work_identity`/`learning_resource`/`resource_role`. Applied local + production (ledger 16). No behaviour change.
- [x] **9.2a — Creator identity + credibility as separated dimensions (2026-07-20).** `packages/research/creator.ts` (provider-metadata-only identity, verification as a category not a score, never invents a creator for an anonymous source) and `credibilityV3.ts` (publication rigor / creator expertise / host provenance / evidence strength / relevance / pedagogical value as independent dimensions; popularity carried separately and never scored — asserted by a test). Migration `0016` (7 columns on `credibility_assessment`), applied local + production (ledger 17). 23 tests (research now 170).
- [x] **9.2b — Research sequence v3 (2026-07-20, commit `dec29bc`).** Ordered v3 stage sequence wired into the worker, strictly gated by `ANALYSIS_PIPELINE=v3`; v2 remains the unchanged production default. Creator verification derives solely from provider metadata; the seven 0016 fields persist on `credibility_assessment`; popularity stays display-only; unsupported influence labels demote to `ai_inferred`. Verified: full workspace typecheck, 251 package/worker tests (170 research, 3 pure v3), 6 local-Postgres lifecycle tests. **Required owner-approved production canary run 2026-07-20** against the private Irwin fixture: `run v3 complete/published`, non-degraded, full structure, 33 research resources, 33 credibility assessments with all separated dimensions populated, cost $0.0557596. All canary production data (67 usage-log rows, 33 graph edges, 33 bibliographic records, 48 cache rows, the copied fixture object, and the disposable canary user) purged and verified back to 0 rows except the retained user; Render's `ANALYSIS_PIPELINE` reverted to `v2` and redeployed, confirmed live via the worker's own startup log line. `v1`/`v2` unaffected throughout.
- [x] **9.3 — Passage-anchored annotations, complete and production-canary-verified (2026-07-20).** `passage_annotation` (migration `0017`): anchored to a real `text_block_id` or explicitly `is_whole_work`, never both and never neither — a DB check constraint makes a fabricated anchor structurally impossible, not just an app-level convention (verified with both raw SQL and a Drizzle-ORM round-trip). `packages/research/passageAnnotations.ts` synthesizes candidates in one bulk call per run (bounded cost regardless of document length); every anchored quote is verified as a real substring of the block it claims (`quoteIsGrounded`, reused from 9.2's synthesis module) or the whole candidate is dropped — the one invariant this feature won't soften. Falls back to an honest empty result (not a heuristic guess) with no model key. Wired into the v3 worker sequence under the (previously label-only) `section-passage-anchors` stage; assembled into `getPublishedEdition()` as `passageAnnotations`/`wholeWorkGuidance`; rendered inline at the exact paragraph in `EditionReader.tsx`, with "Whole-work guidance" always the literal, separate label the plan requires. Verified: full workspace typecheck, 14 `@ice/research` unit tests (fabricated-quote rejection, unknown-block rejection, type/relationship/summary-length demotion, whole-work capping), 4 DB-integration tests proving the Drizzle insert shape and the check constraint both hold, and 2 Playwright tests in the CI-safe `edition.spec.ts` (inline collapse/expand, whole-work separation) — 9/9 passing. Migration `0017` applied to production (ledger 18). **Owner-approved production canary run 2026-07-20** against the private Irwin fixture: run `3872089f` complete/published, non-degraded, 6 grounded passage annotations (all anchored, real quotes verified against real blocks), 32 research resources / 32 credibility assessments, total cost ≈$0.098 across all attempts. The first attempt (run `4bfffa21`) published with zero annotations due to the maxOutputTokens truncation bug fixed in commit `4e13d72`; a second attempt (`fbbdd949`) was orphaned when the Render worker crashed mid-run and pg-boss's 1-hour job expiry hadn't yet elapsed, so it was manually re-enqueued (mirroring the app's own reprocess-route logic) rather than waited out — see the Known Problems entry on pg-boss job expiry. All canary production data purged and verified back to 0 rows except the retained user; Render's `ANALYSIS_PIPELINE` reverted to `v2` and redeployed, confirmed live via the worker's own startup log line. A separate operational incident during this closeout (a Render env-vars PUT that wiped all vars except one, and a DATABASE_URL misconfiguration once restored) is documented in full in Known Problems and `docs/CHANGELOG.md`; the two optional keys it also cost (`GROBID_SA_KEY`, `GOOGLE_BOOKS_API_KEY`) were subsequently regenerated/recovered the same day — production is fully restored, all 11 env vars verified present, and the worker confirmed booting clean on `pipeline v2`.
- [x] **9.4 — Reader levels & concept mastery, complete and production-canary-verified (2026-07-20).** Needed **zero new migration** — every table 9.4 uses (`users.reader_level`, `concept`, `concept_mastery`, `graph_edge`'s already-unused `presupposes` edge type) already existed from `0015`/`0017`. Four pieces: **(1)** Retired `preferences.expertise` (3-level) in favor of `users.readerLevel` (4-level) everywhere it was read — onboarding, `packages/roadmap`'s pure ranker (now `tiersForReaderLevel`, `undergraduate` kept byte-for-byte identical to the old `intermediate` so the one already-backfilled production user sees no behavior change), the roadmap API/UI. **(2)** Real extraction for the v3 sequence's `concepts-people-debates` stage — like `section-passage-anchors` before 9.3, it had only ever been a label with a `[resolvedTitle]` placeholder; `packages/research/concepts.ts` now extracts concepts/doctrines/people/traditions/debates in one bulk call per run, upserted into the global, append-only `concept` catalog by deterministic slug, linked to the work via a new `work -[presupposes]-> concept` `graph_edge` (idempotent-safe on reprocess — `graph_edge` carries no `runId` of its own, unlike `research_resource`/`passage_annotation`). **(3)** `packages/research/mastery.ts` implements the full plan-specified precedence chain as pure functions: `shouldOverwriteMastery` (explicit > diagnostic > inferred, enforced at write time so a weaker signal can never silently downgrade a stronger one already on record), `inferMasteryFromCompletedWorks` (has the reader completed ANOTHER work presupposing the same concept? — the "completed prerequisites, weak evidence only" step), and `defaultMasteryForReaderLevel`/`effectiveMastery` (the final global-level fallback, applied only at read time when nothing is recorded). **(4)** A reader-facing, entirely optional per-work diagnostic (`/works/[workId]/diagnostic`) — self-assessed familiarity per concept (four buckets) rather than an LLM-graded fact quiz, since fabricating "correct answers" for a scholarly domain is exactly the hallucination risk this app's whole posture exists to avoid. The roadmap's level selector now always shows all 5 options (4 levels + explicit "Show all levels") with live per-level counts computed from the same already-fetched candidates (`countByReaderLevel`, no extra DB round trip) — picking one is a page-local view filter only and never overwrites the reader's saved global level, satisfying "browsing alone never silently changes a level." Verified: full workspace typecheck and lint; 206 `@ice/research` unit tests (up from 184; `concepts.ts`, `mastery.ts`) plus 17 `@ice/roadmap` tests; 5 new DB-integration tests proving slug-dedup and edge-idempotency hold against real Postgres; 4 new Playwright tests in the CI-safe `diagnostic.spec.ts` (skippability, the precedence rule holding through the real UI — not just the pure unit test — and the inferred-from-completion pre-fill) — all 18 CI-safe specs green, including the pre-existing 14. **Owner-approved production canary run 2026-07-20** against the private Irwin fixture: run `8abba92a` complete/published, non-degraded, full structure, cost $0.0679858. Real concept extraction confirmed working end-to-end in production for the first time — 10 substantive concepts extracted (e.g. "Decision (prohairesis)" 0.90 confidence, "Virtue of character (aretê) and the doctrine of the mean" 0.86, plus the actual scholars the paper discusses, Julia Annas and Jean Hampton, correctly typed as `person`), each linked to the work via a real `presupposes` graph edge — not the old placeholder. The full mastery precedence chain was then proven against this real data (not just the unit tests): a `diagnostic` self-assessment written for the top concept, the work marked completed, the GET route's opportunistic inferred-write step correctly filled the other 9 previously-uncovered concepts with `inferred`/score-65 rows, and a subsequent attempt to overwrite the diagnostic concept with a weaker `inferred` write was correctly rejected by `shouldOverwriteMastery`, leaving the diagnostic score untouched. 38 research resources/credibility assessments also produced as part of the same run. All canary production data (79 usage-log rows, 1 disposable canary user cascading its work/document/run/resources/concept-mastery/graph-edges/reading-record, 38 orphaned bibliographic records, 44 cache rows, 10 orphaned concepts, the copied fixture object) purged and verified back to 0 rows except the retained user; Render's `ANALYSIS_PIPELINE` reverted to `v2` and redeployed, confirmed live via the worker's own startup log line on a fresh instance. See the Known Problems entry on `render restart` vs `render deploys create` for an operational gotcha this closeout surfaced.
- [x] **9.5 — Library & work grouping, complete and production-canary-verified (2026-07-20).** Migration `0018`: `works.workIdentityId` (nullable FK to `work_identity`, set the first time one of a reader's uploads is analyzed under v3) and a third polymorphic target, `learningResourceId`, on `reading_record`/`understanding_rating` alongside the existing `workId`/`bibId` — "exactly one of the three" enforced with a DB CHECK constraint on both tables, not just app-level convention, same precedent as `passage_annotation`'s anchor-or-whole-work constraint from 9.3 (verified against local Postgres before applying, and again in production after: zero pre-existing rows violated it, and it correctly rejects zero/two/three-target inserts through both raw SQL and the Drizzle ORM). `apps/worker/src/analyze.ts` gained a v3-only promotion block — same containment as every prior Phase 9 sub-phase — that finds-or-creates the primary work's own `work_identity` once per run (racy unique constraint closed with `onConflictDoNothing` + reselect, unlike the plain select-then-insert `bibliographic_records` gets away with) and, per accepted resource, upserts `learning_resource` (by `normalizedKey`) and `resource_role` (by its own unique constraint) — "a later run has better data" refresh semantics, mirroring `bibliographic_records`. Citation stubs and resources with no normalizable title are skipped rather than promoted as unreadable/unkeyable ghost entries. A new `POST /api/library/[resourceId]/status` route mirrors the existing `roadmap/item` route's shape, scoped by `learningResourceId` instead of `bibId`. UI: `/works` (today's uploads list, moved out of `/dashboard`), `/dashboard` rewritten as a light overview (counts, continue-reading nudge, quick links), and a new `/library` — tabs (All/To read/Reading/Completed), filters (relationship/source type/reader level), sort (recency/title/credibility), and a reading-state control per row. Nav is now Dashboard · Works · Library · Graph · Upload. Credibility is joined at read time (`research_resource` → `processing_run` → `document`, scoped to the exact owned work being displayed, not a workKey string match — that would compare the wrong identity) rather than duplicated onto `learning_resource`, avoiding a second staleness problem. Verified: full workspace typecheck/lint; 7 CHECK-constraint integration tests + 4 promotion-idempotency integration tests (`apps/worker/src`); 3 seeded Playwright tests (`library.spec.ts`, CI-safe, no worker/API spend) plus an updated `onboarding.spec.ts` assertion and an extended `security.spec.ts` authorization-matrix case — all 21 CI-safe specs green; real screenshots against a locally seeded v3-shaped user confirmed `/dashboard`, `/works`, and `/library` all render correctly. **Owner-approved production canary run 2026-07-20** against the private Irwin fixture: migration `0018` applied to production first (ledger 19, verified via `information_schema`/`pg_constraint` reads), `ANALYSIS_PIPELINE` flipped to `v3` and redeployed (confirmed via the new instance's own startup log line), then a disposable canary user/work/document seeded directly and the real `extract-text` job enqueued through `enqueueExtractText()` — mirroring the upload flow exactly. Run `82232acf` completed clean: `complete`/`published`, non-degraded, cost $0.0461534. **Real Library promotion verified working in production for the first time**: the primary work's `work_identity` correctly resolved to the GROBID-extracted title "Vice and Reason" (not the filename-derived provisional title), 25 real scholarly sources promoted into `learning_resource` with correct types/providers/peer-review flags, 25 matching `resource_role` rows with a real mix of relationship classifications (`explicit_reference`, `secondary_scholarly_recommendation`, `parallel_comparison`, `ai_inferred`) and confidences, and exact 25/25/25 parity against `research_resource`/`credibility_assessment` for the run. The reading-state write path was also proven against this real data (a `reading_record` correctly written with `learning_resource_id` set and `work_id`/`bib_id` both null, satisfying the new CHECK constraint). All canary production data (53 usage-log rows, the disposable user cascading its work/document/run/resources/reading-record/graph-edges, 25 orphaned learning resources cascading their resource roles, 15 orphaned work identities, 25 orphaned bibliographic records, 10 orphaned concepts, 42 cache rows, the copied fixture object — deleted via the documented direct-Storage-API workaround after `supabase storage rm` silently no-opped) purged and verified back to 0 rows except the one retained non-canary acceptance user; Render's `ANALYSIS_PIPELINE` reverted to `v2` and redeployed, confirmed live via the new instance's own startup log line.
- [x] **9.6 — Curriculum & study guide, complete (2026-07-20). No production canary needed** — 9.6 is a pure read-time view over 9.5's `resource_role`/`learning_resource`, so there was no new migration and no new write path to canary; the same posture Roadmap (Phase 5) took over `graph_edge` once Phase 4's canary had already proved that data gets written correctly. New `packages/curriculum` (mirrors `@ice/roadmap`): the 10 relationship categories collapse into 5 fixed-order pedagogical stages (Prerequisites → Formative context → Core engagement → Interpretation & context → Extension), which makes "acyclic dependencies" true by construction — proved, not assumed, via a general-purpose `hasCycle()` DFS check plus `assertAcyclicStages()`, exercised by both a synthetic-cycle test and a real-output test. Three routes (`minimal|university|graduate`, each a stage superset of the one before with an increasing per-stage cap) reuse the "level only narrows the default" rule 9.4 set for reader levels. Per-item rationale/difficulty/time all reuse existing `resource_role`/`learning_resource` fields rather than re-deriving them; `checkpoint` is the one new piece — a deterministic, template-based self-check sentence per relationship category, same technique as `@ice/roadmap`'s `reasonFor()` (no AI call, no fabrication risk). Completed → review-only reuses `rankRoadmap`'s exact `known` rule, scoped by `learning_resource_id`. Web wiring mirrors existing precedent throughout: `apps/web/src/lib/curriculum.ts` mirrors `roadmap.ts`'s split, `/api/works/[workId]/curriculum` mirrors the roadmap route, `/works/[workId]/curriculum` mirrors `RoadmapView.tsx`, and the reading-status mutation reuses the EXISTING `/api/library/[resourceId]/status` route (no new endpoint). Verified: 17 `@ice/curriculum` unit tests, full workspace typecheck/lint, all pre-existing suites unaffected, and 4 new seeded Playwright tests (`curriculum.spec.ts`, added to the CI-safe list) that caught two real bugs before landing — a missing `documents` row in the new test-seed helper (`getOwnedDocument` needs one; `/library`'s existing helper never did), and a stale review-only badge caused by an optimistic local-state patch instead of a refetch (fixed by adopting `RoadmapView`'s mutate-then-reload pattern). Manually screenshotted both the graduate route (all 5 stages) and the minimal route (correctly narrowed to 2, with a live count) against a locally seeded work. Full narrative: `docs/CHANGELOG.md`.
- [x] **9.7 — Graph, trash, cost UI, complete (2026-07-20). Migration `0019` applied to production (ledger 20), verified clean: column + index present, 0 of 0 production `work` rows affected (production is empty of works post-9.5-purge, matching expectations).** Three self-contained pieces, each its own commit. **(A) Graph:** `buildGraph()` (`apps/web/src/lib/graph.ts`) now also surfaces concept nodes (concept/doctrine/person/tradition/debate — already one typed `concept` table via `concept_kind`) reached through the real `work -[presupposes]-> concept` `graph_edge` 9.4's worker already writes — zero new writes — plus, work-scoped only, a section outline pulled read-time from the primary work's own published `text_block` headers (no cross-work "section" entity invented; sections aren't canonical/shared the way concepts are, a deliberate, documented scoping decision). Filter state (status/kind/relation/authority/provider) was lifted out of the table-only local state it lived in and into `GraphView`, synced to the URL, with one `filterGraphData()` result passed identically to both `KnowledgeGraph3D` and `GraphAccessibleFallback` — closing the gap where the 3D view previously showed the whole unfiltered graph while the table filtered independently. Caught and fixed a real UX defect along the way: the shared "unread" state label read "In library, unread", accurate for reference nodes but nonsensical for a concept node, narrowed to plain "Unread". **(B) Cost UI:** new `apps/web/src/lib/cost.ts`'s `getRunCostBreakdown()` groups the already-existing `ai_usage_log` rows (`runId`/`stage`/`estimatedCostUsd`, tracked since Phase 4/8) by stage — no new tracking, a read over data the pipeline already logs — wired into `getPublishedEdition()` and rendered as an expandable disclosure behind the reader's existing "AI cost $X" line. A static, honest pre-run estimate ("Typically $0.50–$2, hard cap $5", the plan's own §34.5 posture) now sits next to Reprocess — never a fabricated per-work prediction. Verified directly in `apps/worker/src/analyze.ts` that `degraded` is defined as exactly `overSoftCap(budget)` — the hard-stop mechanism already existed and already surfaced via `degraded`/`saturationNote`; the default tooltip now names the cause ("cost limit reached") instead of a generic "stopped early". **(C) Trash:** migration `0019` adds `works.deleted_at` (nullable timestamp, indexed) — additive, no new table; deleting a `work` row today cascades through nearly the entire per-document pipeline, which is the actual argument for a recovery window. `getOwnedDocument()` (`apps/web/src/lib/works.ts`) now excludes trashed works, covering ~19 call sites in one place (reader/roadmap/curriculum/graph/diagnostic/reprocess/analyze/edition/annotations/notes/bookmarks/highlights/position); `getOwnedWork()` is the new trash-aware variant for the work's own detail page and the trash routes themselves. Every other works listing (dashboard, `/works`, the reader's work picker, Library, and the graph/roadmap "in library" matches) got the same `deleted_at IS NULL` exclusion added directly. New `DELETE /api/works/[workId]` (soft-delete), `POST .../restore`, `POST .../purge` ("delete permanently now" — an explicit user choice, never automatic), `GET /api/works/trash` (lists + opportunistically purges anything past its 30-day window). **Deliberately no new pg-boss scheduled queue or Render cron for the purge** — trash is pure web CRUD with no AI/worker involvement, so a new scheduled-job queue would be real production infrastructure for a feature that doesn't need it; an opportunistic check-on-load is simpler and idempotent by construction (a `WHERE deleted_at < cutoff` match is harmlessly empty on a repeat run). `WorkStatusPanel` gained a two-step "Move to trash" confirm and a distinct "In trash" banner with Restore; new `/works/trash` page lists trashed works with days-remaining. Extended `security.spec.ts`'s IDOR matrix with the three new mutation routes, and closed a gap from 9.6 in the same pass: the curriculum read route had never been added to that matrix either. Verified: full workspace typecheck/lint/build, all pre-existing suites unaffected, 3 new `graph.spec.ts` tests (concept/section rendering, URL-persisted filter, identical table/3D counts) + 3 new `trash.spec.ts` tests (trash → hidden + 404s on gated routes → listed with 30 days remaining; restore; delete-permanently-now verified at the DB row level, not just the UI) — all CI-safe (pure DB reads/writes, no worker/AI spend), added to CI. Manually screenshotted all three pieces (graph table + 3D + filtered, the cost breakdown disclosure, and the full trash lifecycle) against locally seeded data. **No AI-pipeline canary needed for any of the three pieces** (first Phase 9 sub-phase where none of A/B/C touch the worker). Migration `0019` applied to production via `supabase db query --linked` (a simpler path than the Management-API-`curl` recipe this file has documented since Phase 4 — the CLI's own `--linked` flag authenticates through its stored credential and needs no manually retrieved access token) after explicit owner approval, and re-verified via `information_schema`/`pg_indexes` reads. Full narrative: `docs/CHANGELOG.md`.

## Phase 10 (Workspace Depth & Adaptivity Completion) — sub-phase detail

Moved from `docs/PROJECT-LOG.md`'s "Completed Tasks" section on 2026-07-22, same reason as above.

- [x] **Renumbering and attribution scrub (2026-07-20).** 9.8 "Comprehensive dossier" retired as a phantom requirement (confirmed unenumerated anywhere despite being referenced by count); Phase 9 marked fully complete at 9.1–9.7; a new Phase 10 (this one) takes its place, scoped to closing the concrete gaps 9.1–9.7 left in Phase 9's own original objective; the old Phase 10 (separate-site proposal Site) renumbered to Phase 11. Separately, a full attribution/privacy scrub was carried out across every tracked file and, in one final closing step, git history itself: the canonical project log was renamed from `CLAUDE.md` to `docs/PROJECT-LOG.md` (local tooling still served via a gitignored, never-committed mirror at the repo root), and every mention of the AI coding tool used to build this project — and of the publicly-known reference project studied for architectural ideas during planning — was removed from tracked file content, without touching any string describing the product's own AI/LLM features (OpenAI/Anthropic-as-provider config, "AI-assisted"/"AI-inferred" labels), which remain exactly as they were. **History rewrite (2026-07-20):** `git filter-repo` stripped the `Co-Authored-By`/`Claude-Session` trailer from all 124 affected commits and replaced literal `CLAUDE.md`/`Claude.md` filename mentions in commit messages with "the project log"; all 13 `phase-*-complete` tags were recreated on their rewritten equivalents (one tagging mistake caught and fixed in the process — `phase-10-complete` had landed one commit early); force-pushed to `origin main` after a final explicit review of the rewritten log and tags. This scrubs commit messages everywhere and file content from this session's commits forward; it does not retroactively rewrite the *content* of old file blobs in earlier commits (e.g. an old tagged snapshot's `CLAUDE.md` blob still shows old content under its old historical name) — a documented limit, not an oversight, and not pursued further since this is a private repo with no external clones.
- [x] **Pure-UI scope items, complete (2026-07-20). No migration, no canary — plan §35.2's audited pure-UI/AI-pipeline split.** Four items, each web-only: **(1) Annotation relationship/link.** `passage_annotation.relatedResourceId` was already selected by `getPublishedEdition()`'s `db.select()` but silently dropped in the output-mapping step (`apps/web/src/lib/edition.ts`); now passed through, `EditionPassageAnnotation.relationship` retyped from a loose `string` to the real `RelationshipCategory` union, and `PassageAnnotationNote` (`EditionReader.tsx`) renders both a relationship badge (reusing the existing `CATEGORY_META` glyph/label/color, no new labels invented) and a link to the related resource when one is set, resolved through the `resourceById` map the component already built for generated-note evidence. **(2) Annotation filter/sort.** `AnnotationsPanel.tsx` gained category and review-status filters plus a sort control (confidence/category/review-status), mirroring `LibraryView.tsx`'s established filter-bar pattern; re-sorting the confidence-desc list is safe because `Array#sort` has been spec-stable since ES2019, so ties still fall back to the server's original order. **(3) Library default-scoping.** `LibraryView`/`/library`'s `page.tsx` now seed the reader-level filter from `getUserReaderLevel()` (defaulting to `"all"` when unset) instead of always starting at an unscoped "All levels", with the same "never hides anything — pick Show all levels anytime" note and per-level counts Roadmap already established; picking a different level on the page is still a page-local view filter only, never a write to the saved profile. **(4) 3D graph visual richness**, deliberately reversing the earlier restraint decision: `KnowledgeGraph3D.tsx` gained directional link particles (`linkDirectionalParticles`, previously `0`), hover-highlight of the neighbor subgraph (adjacency computed once per data change, non-neighbors dimmed via `nodeColor`/`linkColor`/`linkWidth` rather than a nonexistent per-node opacity accessor), and click-to-focus camera fly-to via a `ForceGraphMethods` ref and `cameraPosition()` — still driven by the same `STATE_META` palette tokens and still a single damped transition, no auto-rotation, no repeated motion; the accessible table is completely unchanged. Verified: full workspace typecheck/lint/build green, and the 15 pre-existing CI-safe `edition.spec.ts`/`graph.spec.ts`/`library.spec.ts` Playwright tests (seeded, no worker/AI spend) all still pass unmodified against the live dev server — no test needed updating, confirming these were additive UI changes with no behavioral regression.
- [x] **Jargon-extraction tightening, complete and production-canary-verified (2026-07-20).** The passage-annotation extraction prompt (`packages/research/src/passageAnnotations.ts`) previously listed "a difficult term" as one of four equally-weighted example categories, with no instruction to actively scan for it — a real under-indexing risk against plan §35.2's "reader shows clickable highlights explaining terms, jargon, foreign concepts" requirement. Tightened to instruct the model to actively scan every block for terminology a reader would stumble on (technical jargon, terms of art, untranslated foreign phrases, acronyms, assumed-familiar proper names) as the single most valuable annotation category, explicitly never to be crowded out by the more generic ones, and to define a "definition"-type annotation's explanation as the term's meaning AS USED HERE rather than a generic dictionary gloss. Pure prompt-text change — no schema, no new field, no test asserts the literal prompt string. Verified: full `@ice/research` typecheck and all 206 existing unit tests unaffected. **Owner-approved production canary run 2026-07-20** against the private Irwin fixture: `ANALYSIS_PIPELINE` flipped to `v3` via the Render dashboard/API (env-var PUT confirmed to touch only that key; new instance's own startup log line confirmed `pipeline v3`), a disposable canary user/work/document seeded directly and the real `extract-text` job enqueued via a direct pg-boss partition-table insert (mirroring `enqueueExtractText()`, since the production `DATABASE_URL` password isn't recoverable — see Known Problems). Run completed clean: `complete`/`published`, non-degraded, full structure, 29 research resources, 29 credibility assessments, 7 passage annotations, cost $0.051906. **4 of the 7 annotations were correctly typed `definition`**, capturing real Greek/Latin technical vocabulary the paper actually uses (*prohairesis*, *to logon echon*, *kakia*/*mochtheria*, Aquinas's *certa malitia*) — a direct, real-data confirmation the tightened prompt targets exactly what it was meant to. All canary production data (61 usage-log rows, 38 graph edges, 29 orphaned bibliographic records, 29 orphaned learning resources + their resource roles, 21 orphaned work identities, 38 research-cache rows, the disposable user cascading its work/document/run, and the copied fixture object in Storage — see the one exception below) purged and verified back to 0 rows except the retained non-canary acceptance user; `ANALYSIS_PIPELINE` reverted to `v2` and redeployed, confirmed live via the new instance's own startup log line. **One cleanup exception**: `supabase storage rm` silently no-opped on the copied fixture object (the same documented gotcha as the 9.5 closeout), and the same-bucket-only `storage cp` this session confirmed cross-bucket copy is unsupported by the CLI (worked around with a download-then-upload instead — a genuinely new finding, now recorded in Known Problems); the one orphaned object needs a manual delete via the Supabase dashboard.
- [x] **Structural evidence-strength scoring, production-canary-verified in the same run above (2026-07-20).** The rationale strings on real production `credibility_assessment` rows confirmed the new `structuralEvidenceStrength()` signal is live and descriptive ("no abstract/snippet available", "abstract present, no structural signals found") in place of the old bare `evidenceStrength 0.30`/`0.60` numbers with no explanation — exactly the mechanism this feature added, verified against real Crossref/OpenAlex philosophy-article abstracts (none of which happened to contain RCT/sample-size/statistics language, which is expected and correct for this subject matter, not a defect).
- [x] **Suggested reader level, complete (2026-07-20). No migration, no worker touch, no canary needed** — architected as a pure read-time inference over data already persisted (Library completion status + each item's own reader level), the same posture 9.6's Curriculum took over `resource_role`/`learning_resource`, deliberately deviating from the plan's own framing of this item as "touches the worker" (documented here rather than silently done differently: there was no actual need for a new AI call or worker involvement once the feature was scoped as an inference over existing structured data, not a new classification). `suggestReaderLevelFromCompletions()` (`packages/roadmap/src/index.ts`) suggests the highest reader level with at least 2 completed Library items at that level, strictly above the reader's current saved level (or "beginner-and-below" if never chosen) — evidence only ever nudges upward, mirroring 9.4's `inferMasteryFromCompletedWorks` precedent. `LibraryView.tsx` surfaces it as a dismissible banner ("Switch to X" / "Dismiss"); Switch calls the new `POST /api/reader-level` route (writing `users.readerLevel` via the existing `setUserReaderLevel()`) and Dismiss is remembered per-suggested-level in `localStorage` so the identical suggestion doesn't keep reappearing — browsing/dismissing never silently changes the saved profile level, only an explicit Switch click does. Verified: 6 new `@ice/roadmap` unit tests (23 total, up from 17) covering the threshold, directionality, and null-current-level cases; full workspace typecheck/lint/build green; 2 new CI-safe `library.spec.ts` Playwright tests (suggestion appears and is dismissible/persists across reload; Switch actually writes `users.readerLevel`, verified at the DB row level) — all 5 library specs passing.
- [x] **Reference-project-informed technique adoption, complete (2026-07-20, plan §35.3) — one adopted, one already satisfied, two assessed and honestly declined.** Four techniques were surveyed as ideas only (never named, never copied); each was independently audited against this codebase before writing any code, in the same spirit as the relevance-gate/evidence-strength premise-checks below. **(1) Two-stage cost pre-filter ahead of the relevance gate's LLM calls — assessed, not applicable.** `packages/research/src/relevance.ts`'s own file header states the gate is "deterministic and unit-testable with no network and no LLM" — confirmed by reading `assessCandidate` in full: there is no per-candidate LLM call in the relevance gate to filter ahead of, so a pre-filter there would save nothing. The real per-candidate cost is `classifyRelationship`/`synthesizeNote` in `apps/worker/src/analyze.ts`, both of which already run only on gate-accepted candidates, already capped at `maxFullInspections` (120), and already `canAfford`/`overSoftCap`-gated — the cost control this technique would add already exists, just implemented differently than assumed. **(2) Deterministic evidence-strength scoring from structural cues — adopted.** Same premise-check pattern: `evidenceStrength` turned out to be a binary snippet-presence heuristic (`r.snippet ? 0.6 : 0.3`, `apps/worker/src/analyze.ts`), not an LLM judgment at all, so there was no "reduce LLM reliance" angle — but replacing a crude binary check with real structural signal extraction is a genuine, standalone improvement on its own merits. New `structuralEvidenceStrength()` (`packages/research/src/credibilityV3.ts`) scans a scholarly article's abstract for a named study design (RCT, meta-analysis, cohort study, etc.), a reported sample size, inferential statistics (p-values, confidence intervals, effect sizes), and hedging language — scoped to scholarly-article resources specifically, since these regex classes don't meaningfully apply to a web page or video, which keep the prior existence-based fallback unchanged. Wired into both the legacy and V3 credibility call sites in `analyze.ts`, with the "why" folded into `assessCredibilityV3`'s rationale string alongside every other dimension's. **(3) Visual language for the knowledge graph — already satisfied** by the Workstream 2 graph-richness item above (directional link particles, hover-highlight, click-to-focus), which was scoped from the start to stay on `react-force-graph-3d` with no custom engine, per this technique's own framing. **(4) Concurrent LLM calls in the worker's per-document pipeline — assessed, not adopted.** Provider-level concurrency across the discovery adapters *already exists*: `runDiscovery()`'s round loop (`packages/research/src/discover.ts`) already calls every active adapter via `Promise.all` per round, not sequentially — the "~5× wall-clock speedup" this technique targets is already realized there. The one remaining sequential stage is the per-candidate loop in `analyze.ts` (classification → credibility → note synthesis → a chain of foreign-key-dependent DB inserts, repeated per candidate) — parallelizing it would race the shared `CostBudget`'s check-then-charge pattern (`canAfford()`/`charge()` in `discover.ts`) across concurrent `synthesizeNote` calls, and risk non-deterministic write ordering across inserts that depend on each other's returned ids. This is exactly the risk the plan's own caveat flagged ("needs a concurrency-safe accounting change first") — declined rather than forced, consistent with "adopt only if there's a real, measured win and no correctness risk." Verified: 9 new `@ice/credibilityV3` tests (215 `@ice/research` tests total, up from 206) covering each structural signal individually and in combination, plus the rationale-string threading; full workspace typecheck/lint/test/build green.

## Phase 11 (Rebrand, UI/UX Overhaul, Reader/Relevance Fixes) — sub-phase detail (11.1–11.5)

Moved from `docs/PROJECT-LOG.md`'s "Completed Tasks" section on 2026-07-22, same reason as above. Sub-phases 11.6–11.9 were never duplicated in "Completed Tasks" and are preserved below in the "Remaining Tasks (historical, resolved)" section instead.

- [x] **11.1 — Branding rename to "Palimnote" (2026-07-21).** New `apps/web/src/lib/brand.ts` exports `SITE_NAME`/`SITE_TAGLINE`, imported into every file with a literal "Interactive Critical Edition" occurrence: root layout metadata, the authenticated-app nav header, `SiteHeader`/`SiteFooter`, privacy/terms page metadata, `auth-service.ts`'s verification/reset email subjects, and `mail.ts`'s email bodies — all now reference the one constant rather than a repeated string literal. The landing page's two lowercase/generic "critical edition" mentions (describing the artifact type, not the brand) were reworded rather than replaced, so the copy doesn't read as if "critical edition" is a second product name — one now uses `SITE_NAME` directly ("Palimnote turns it into an annotated edition —"), the other stays generic ("an annotated critical edition"). Backend/DB/package/repo names are unchanged (explicitly out of scope per the plan). Verified: full `pnpm --filter web typecheck`/`lint` green; extended `landing.spec.ts` with an assertion that the header renders a "Palimnote" link.
- [x] **11.2 — Theming: harsh/bright functional accents + credibility meter (2026-07-21).** Two new functional accent tokens (`--color-credibility-critical`/`--color-credibility-warning`, more saturated than the rest of the warm palette, with distinct light/dark values) added to `globals.css`'s three token blocks and registered in `@theme inline`; "good" reuses the existing `--color-accent-green` rather than inventing a second green — scoped deliberately to credibility alerting only, not a general brightness increase. New `apps/web/src/components/CredibilityMeter.tsx`: a 3-band (not continuous-gradient) horizontal bar from `credibility.score`, always paired with a text label (never color alone), wired in alongside — not replacing — the existing letter-grade `AuthorityBadge` at both plan-named consumer sites: `EditionReader.tsx`'s Sources-consulted list and `LibraryView.tsx`'s item rows (the third planned consumer, the Visualization node-detail panel, doesn't exist yet — deferred to 11.9). General contrast turn-up: `:focus-visible` outline widened 2px → 3px; light-theme `--color-text-muted`/`--color-border` darkened, dark-theme equivalents lightened, in all three token blocks. Collapsed the `CATEGORY_META`/`.reader-annotation-*` duplication flagged by the plan: `AnnotationMarkerAnchor` now carries a `colorVar` (from `CATEGORY_META`, the one source of truth) instead of a bare category string, `highlightDom.ts`'s `applyAnnotationMarkers` sets it as an inline `--reader-annotation-color` custom property per marker, and the ten hand-kept `.reader-annotation-{category}` CSS class rules in `globals.css` are deleted in favor of one rule reading the custom property; the landing page's static example marker updated to match. Verified: full workspace `pnpm -r typecheck` and `pnpm --filter web lint` green, `pnpm --filter web build` succeeds. No visual-regression suite exists in this repo (plan's own verification table says manual QA only); no e2e spec referenced the deleted classes.
- [x] **11.3 — Upload: honest progress indicator (2026-07-21).** Two independent pieces, no backend/schema change. **(1) Real byte-level transfer progress:** `apps/web/src/app/(app)/upload/page.tsx`'s primary direct-PUT path swapped from `fetch` (no upload-progress event exists on it) to `XMLHttpRequest`'s `upload.onprogress`, rendering a native `<progress>` bar plus a "NN% · X.XMB of Y.YMB" line; the small-file proxy/base64 fallbacks are unchanged, still on `fetch`, exactly as scoped. **(2) A discrete, honest step list for server-side processing — a real finding along the way: this needed no new polling logic.** Reading `apps/worker/src/index.ts`'s `handleEditionExtraction` closely (the actual `extract-text` job handler for v2/v3) showed the *entire* edition pipeline — extraction AND the full research/classification sequence — runs inside that one job, with `document.processingStatus` staying `"processing"` the whole time and only flipping to `needs_review`/`ready` at the very end in `publishEditionRun`; `WorkStatusPanel`'s existing polling already covers this whole window (`POLLING_STATUSES` includes `"processing"`), it just wasn't rendering the `processing_run.stage` value as anything but a bare interpolated string. New `packages/config/src/stages.ts`: `V2_STAGE_SEQUENCE` (5 stages, read off the real `setStage()`/direct-update call sites in `analyze.ts`/`runLifecycle.ts` — `extracting → research-discovery → relevance-gate → classification → validation`) and `V3_STAGE_SEQUENCE` (the pre-existing 12-stage sequence, relocated here from `apps/worker/src/v3.ts` verbatim), plus a `STAGE_LABEL` human-readable map and `stageSequenceForPipeline()`. `apps/worker/src/v3.ts` re-exports `V3_STAGE_SEQUENCE`/`V3Stage` from `@ice/config` unchanged, so `v3.test.ts`'s existing import needed no edit. `apps/web`'s status route and the work-detail server component both now select `processingRuns.pipelineVersion`; `WorkStatusPanel`'s new `StageProgress` component renders the ordered list with completed steps checked, the current stage highlighted, and future steps dimmed — never a fake percentage. v1 has no entry (and none needed — `isEditionPipeline()` never runs the edition pipeline for it, so no `processing_run` row exists to track). Verified: new `packages/config/src/stages.test.ts` (4 tests) plus the existing `apps/worker/src/v3.test.ts` (3 tests, unmodified, still passing against the re-export) — both pass; full workspace `pnpm -r typecheck`, `pnpm -r test`, `pnpm --filter web lint`, `pnpm --filter web build` all green.
- [x] **11.4 — Library work-scoping (2026-07-21).** Pure client-side addition to `LibraryView.tsx`, no backend/schema change — `LibraryItem.recommendedFor` (`{workId, title}[]`) already carried everything needed. New "Work" filter (`aria-label="Work"`, to avoid a `getByLabel` collision with each row's `"Reading status of <title>"` select once titles share substrings) alongside the existing filter bar, its options built by deduping `recommendedFor` across all items; the `visible` `useMemo` gained one more clause (`i.recommendedFor.some((w) => w.workId === workId)`). Deliberately reuses the Library's own real relatedness mechanism (`resource_role`/`recommendedFor`) rather than the Graph's different, fuzzy-title-matched `graph_edge` mechanism, which would give an inconsistent second answer to the same "what relates to this work" question. Extended `library.spec.ts` with a new seeded test (two works via two `seedWorkWithLibraryItem` calls for one user, asserting the filter narrows to one and "All my works" restores both) — run against the real local dev stack (Postgres + `next dev`, no worker needed): all 6 `library.spec.ts` tests pass. Full workspace typecheck/lint also green.
- [x] **11.5 — Reader overhaul, part 1: EditionReader becomes primary + sidebar infrastructure (2026-07-21).** No schema/worker change. **Flipped the default**: `ReaderShell.tsx`'s edition-fetch effect now sets `showEdition(true)` the moment a real edition arrives, so the published edition is what a reader with one sees first; v1-only works (no edition) are unaffected — the interactive reader stays their only option, unchanged. **Fixed the toggle**: the old single button whose label named the *destination* ("Published edition" while already showing it) is now a two-button segmented `role="group"` control (mirrors the Graph's table/3D toggle pattern exactly) showing current state via `aria-pressed` on both buttons unambiguously. **New sidebar** `EditionAnnotationsPanel.tsx`, modeled on `AnnotationsPanel.tsx`'s shell (`w-80`, border, scroll), swapped in via `ReaderShell` when `showEdition` is true, sharing the existing `showAnalysis` show/hide state — two tabs, "Annotations" (whole-work guidance + anchored passage annotations, ported off the old unconditional below-paragraph list) and "Sources" (the "Sources consulted" list, relocated so a long document no longer needs scrolling past the whole text to reach it; a "Notes" tab for critical notes/footnotes arrives in 11.6). **Ported the click-to-reveal interaction**: `EditionReader`'s block `<p>` elements are now refs `applyAnnotationMarkers` (reused unchanged from `highlightDom.ts`, the same mechanism `TextReader` already uses) writes into directly — real quote-fingerprint matching against real `textBlockId`s, degrading gracefully to first-occurrence-in-block when `prefix`/`suffix` are empty (passage annotations don't carry those, only `quote`, and a block's search space is small enough that this is a non-issue in practice). Clicking a marker calls the same `openAnnotation` callback `TextReader` already used (`setShowAnalysis(true)`, `setActiveAnnotationId(id)`) — one shared code path for both readers. A clicked/active card auto-expands its explanation (`expanded = open || active`, deliberately derived rather than effect-set state, to satisfy the lint rule against `setState` inside an effect body — same fix applied to the sidebar's tab-switch-on-click sync, which genuinely needs the one-shot external-prop-sync pattern this codebase already has precedent for in `LibraryView.tsx`). **New hover preview** (neither reader had this before): `AnnotationHoverPreview.tsx`, a lightweight fixed-position popover shown via `onMouseOver`/`onMouseOut` delegation on the article container, keyed off the same marker elements — the click path remains fully sufficient on its own for touch devices, which never fire hover. **Old `PassageAnnotationNote` retired**; its rendering logic moved into the new file's `PassageAnnotationCard`, and `AuthorityBadge`/`AUTHORITY_LABEL`/`AGREEMENT_LABEL`/`PASSAGE_TYPE_LABEL`/`READER_LEVEL_LABEL`/`Authority`/`Agreement` are now exported from `EditionReader.tsx` so the sidebar (and the still-in-page "AI-generated critical notes" section) can share them without duplicating badge logic. Verified: full workspace `pnpm -r typecheck`, `pnpm --filter web lint`, `pnpm --filter web build` green; `edition.spec.ts` substantially extended (10 tests, up from 7) — three tests rewritten to check the new sidebar location (Sources tab, in-text-marker-then-sidebar-card, whole-work-guidance-in-sidebar) rather than the old in-page sections, two new tests added (the flip-default itself, exercised implicitly on every test via a rewritten `beforeEach` that asserts the edition is visible with no click at all; and an explicit segmented-control `aria-pressed` state-transition test) — all 10 pass against the real local dev stack, plus all 35 CI-safe specs across the whole suite still pass unmodified/passing.

## Remaining Tasks (historical, resolved)

Moved from `docs/PROJECT-LOG.md`'s "Remaining Tasks" section on 2026-07-22 (that file exceeded a 150,000-character tooling limit). These are all `[x]`-checked, i.e. already completed; `docs/PROJECT-LOG.md` keeps a condensed one-line pointer for each. Order preserved exactly as it appeared there.

- [x] **Fixed the invalid-password login path (Phase 8.1):** `loginAction` now wraps `signIn` in try/catch, maps `AuthError` → the friendly `/login?error=1` state, and re-throws the `NEXT_REDIRECT` control-flow error. Regression test added.
- [x] **Clean-slate production purge + private Irwin eval fixture (2026-07-19)** — all three uploads permanently removed, library/graph empty, account retained; canonical Irwin PDF preserved in the private `eval-fixtures` bucket with a recorded SHA-256. See the changelog entry for exact counts. Local test-upload reset still pending until local Postgres is next started.
- [x] **Irwin gold-eval steering package (2026-07-19)** — `docs/eval/irwin-vice-and-reason/` holds the fixture record, the supplied brief (with its ~24 transmission-damaged spans marked, never silently repaired), a machine-readable coverage manifest with empirically-observed negative examples, and a full independent verification (24/24 bibliographic assertions resolved, 12/12 substantive claims confirmed against the paper's own text, 3 errors in the brief caught, 4 open questions preserved). The PDF is not in git.
- [x] **Phase 8 relevance closeout — gate built and calibrated (2026-07-19).** Migration `0013_phase_8_candidate_verdicts` applied **locally**; `packages/research/src/relevance.ts` assesses every candidate before any authority scoring; the worker persists all verdicts and projects only `accepted` ones. Zero false accepts on the 22 observed Irwin cases; 36 tests.
- [x] **Lane-specific discovery + research-review UI (2026-07-19).** `generateLaneQueries`/`heuristicLaneQueries` emit per-lane rounds (explicit citations first); `providersForLane` routes each lane only to providers that can serve it; `runDiscovery` tags every resource with the lane that first surfaced it and still records an honest attempt for adapters no lane consulted. `/admin` gained a "Source relevance review" section (accepted/quarantined/rejected counts, per-lane×verdict breakdown, displayed-precision, and the quarantined list with reasons).
- [x] **Live gold-eval canary run (2026-07-19).** Three production runs over the Irwin fixture, $0.124 total. Caught and fixed two real defects (filename-as-identity; author surname as core concept), then measured the gate honestly: **displayed precision 100% (4/4)** on the final run.
- [x] **Relevance recall fixes + canary run 4 (2026-07-19).** Entity evidence now read from abstract/venue (gated on title vocabulary); citation matching corroborated by author+year. **8/8 accepted correct — precision 100%, recall doubled**, and Nielsen 2017 now surfaces.
- [x] **Note-style citation extraction built (2026-07-19).** Quoted-title and book-form note citations, matched whole-text and line-wrap tolerant: 0 → 9 citations on the Irwin fixture. GROBID cannot help here (4 notes of ~40, no bibliography, `bibr` refs are Bekker numbers).
- [x] **Render deploy gap resolved (2026-07-20).** Cause was a **GitHub outage** breaking Render's git auth, not a lapsed connection — deploys from `b01fa13` to `d35e54a` were `build_failed` while `2236ed8` kept serving, which is why canaries 5–7 measured stale code. Retried after the outage eased: `dep-d9eni83rjlhs73cp69b0` is **live on `447b434`**, so the note-style citation extractor is finally in production.
- [x] **Explicit-citation recall largely solved (2026-07-20, canary 10).** Root cause was `first(queries)` in every adapter, not source coverage: the explicit-citation lane now issues one lookup per cited work. Reference-entry matches 1 -> 20; 7 of 9 extracted citations resolve (~88% vs a >=95% gate).
- [x] **Work-level identity built (2026-07-20).** `workIdentity.ts` + migration `0014_work_identity` (local + production). Reviewer framing and edition markers stripped, work-author resolved over reviewer, records grouped so the Library shows one entry per work with reviews attached. 18 tests from real accepted titles.
- [x] **Work grouping shipped to the reader (2026-07-20).** Edition API exposes `works`; Sources consulted lists one entry per work with reviews nested. Verified in production: 21 records -> 12 works.
- [x] **Superseded note (resolved by Phase 16): footnote/bibliography extraction.** The original Irwin limitation remains historical context, but structural ingestion now preserves the footnotes GROBID yields plus captions, bibliography/reference entries, and endnotes as distinct blocks; the processed body excludes all apparatus. Any layout it cannot reliably establish is shown as `structure-limited`, not silently merged. The Phase 16 production PDF canary verified four anchored footnotes and zero footnote-body duplicate matches.
- [x] **Phase 8 acceptance gates met (2026-07-20).** Gold eval: thirteen production canary runs over the Irwin fixture. Expanded v2 Playwright E2E: `edition.spec.ts`, 7 tests, now in the CI-safe set. 10-PDF load test: 10/10 published at full structure, 0 failures, $0.48, 134s/document, 298 MB peak of 512 MB — and it caught two real defects, both fixed.
- [x] **Tagged `phase-8-complete` (2026-07-20)** after confirming both load-test fixes live in production: a full-structure run now reports `degraded=false` with the saturation note kept as information, and produces a single run with no duplicate.
- [x] **Phase 9.1 — Foundations & flag (2026-07-20).** Both traps handled: the exact-equality `ANALYSIS_PIPELINE` checks are gone (new `@ice/config`, `isEditionPipeline()`), and migration `0015` lands the four-level `reader_level` with the `intermediate → undergraduate` backfill plus `concept`, `concept_mastery`, `work_identity`, `learning_resource`, `resource_role`. Applied local + production. No behaviour change.
- [x] **Phase 9.2a — creator identity + credibility as separated dimensions (2026-07-20).** `packages/research/creator.ts` and `credibilityV3.ts`, migration `0016` (7 columns on `credibility_assessment`), 23 tests including one asserting popularity cannot move any score. Pure logic only — the v3 orchestrator that consumes it is 9.2b.
- [x] **Phase 9.2b — research sequence v3 (2026-07-20).** V3 records the approved ordered stages; v2 is unchanged. Creator verification derives solely from provider metadata, the seven 0016 fields persist on `credibility_assessment`, popularity stays display-only, and unsupported influence labels become `ai_inferred`. Unit and local-Postgres lifecycle tests pass; the owner-approved production canary ran clean (33 resources, $0.0557596) and its data was fully purged, Render reverted to `v2` — see the closeout changelog entry.
- [x] **Phase 9.3 — passage-anchored annotations, complete and production-canary-verified (2026-07-20).** Migration `0017` (`passage_annotation`, DB-enforced anchor invariant), `packages/research/passageAnnotations.ts` (grounded quotes only, one bulk call per run, honest empty fallback), worker wiring behind v3, edition assembly + inline reader rendering with a literal "Whole-work guidance" section. 14 unit + 4 DB-integration + 2 Playwright tests, all passing. Production canary against the private Irwin fixture ran clean (6 grounded annotations, 32 resources, non-degraded, ≈$0.098), data purged, Render reverted to v2 — see the 9.3 closeout changelog entry for the full narrative, including a self-inflicted Render env-var incident recovered during closeout.
- [x] **Regenerated the two Render-only secrets lost in the 9.3 closeout incident (2026-07-20).** `GROBID_SA_KEY`: minted a fresh key for the existing `grobid-invoker@ice-grobid-502905.iam.gserviceaccount.com` service account (confirmed it still holds `roles/run.invoker` on the private `grobid` Cloud Run service — no IAM rewiring needed, just a new key). `GOOGLE_BOOKS_API_KEY`: turned out not to be lost at all — Google API keys, unlike the SA key, can be re-fetched from GCP after creation (`gcloud services api-keys get-key-string`), so the existing `ice-books` key was simply retrieved rather than regenerated. Both applied to Render in one complete 11-var `PUT` (never a partial one), redeployed, and the worker's startup log confirmed a clean boot on `pipeline v2` with no crash. Full live-call verification (an actual GROBID request authenticated with the new key) wasn't forced through — the sandbox's safety classifier declined `gcloud auth print-identity-token --impersonate-service-account`, and structural verification (valid JSON, correct `client_email`, confirmed IAM binding) was reasonable evidence without it; the next real document extraction is the final proof.
- [x] **Phase 9 (Interactive Learning Workspace)** — **plan of record: `docs/architecture/plan.md` §34** (approved 2026-07-20, **complete 2026-07-20, tagged `phase-9-complete`**). Delivered as sub-phases 9.1–9.7 behind `ANALYSIS_PIPELINE=v3`, v2 retained as rollback, **core edition first**. `resource_role`/`learning_resource` are populated by 9.5's worker promotion step (v3-only, same containment as every prior sub-phase) and surfaced at `/library`, confirmed with real production data in the 9.5 canary. Scope: Canonical work identity + global learning resources, per-run evaluation, creator/credential evidence, separated credibility dimensions (publication rigor / creator expertise / host provenance / evidence strength / relevance / pedagogical value / popularity-shown-separately), passage anchors, concept–doctrine–person–tradition–debate graph, curricula with acyclic dependencies, learner profile + concept mastery, and 30-day work trash. Reader/Library/Curriculum/Graph share canonical IDs; four reader levels (Beginner/Undergraduate/Advanced/Research) change defaults without ever removing access. Cost: core $0.50–$2 (hard cap $5). **9.8 "Comprehensive dossier" retired before implementation** — an exhaustive grep across the plan, this log, and the changelog found its "14 modules" referenced by count everywhere and enumerated nowhere, and given the real AI-spend stakes attached to it, guessing at scope was rejected in favor of retiring it outright; Phase 9's own original objective is completed instead by Phase 10 below.
- [x] **Phase 10 (Workspace Depth & Adaptivity Completion)** — **plan of record: `docs/architecture/plan.md` §35** (approved 2026-07-20, **complete 2026-07-20, tagged `phase-10-complete`**). Returned to and completed Phase 9's own original objective — passage-anchored annotations, a Library of every recommended source, curriculum routes, an entity-rich knowledge graph, and safe deletion, adapting to reader level without ever hiding depth — by closing the concrete gaps 9.1–9.7 left open: annotation relationship/source linking, annotation filter/sort, Library reader-level default-scoping, 3D graph visual richness, a suggested-reader-level nudge, and jargon/terminology-extraction tightening. See §35.2 for the full audited scope table. The two AI-pipeline-touching items (suggested reader level ended up needing none; jargon-extraction tightening) were verified against a real owner-approved production canary.
- [x] **Phase 11.1 — Branding rename to "Palimnote" (2026-07-21).** See the Completed Tasks entry above for detail.
- [x] **Phase 11.2 — Theming: harsh/bright functional accents + credibility meter (2026-07-21).** See the Completed Tasks entry above for detail.
- [x] **Phase 11.3 — Upload: honest progress indicator (2026-07-21).** See the Completed Tasks entry above for detail.
- [x] **Phase 11.4 — Library work-scoping (2026-07-21).** See the Completed Tasks entry above for detail.
- [x] **Phase 11.5 — Reader overhaul, part 1: EditionReader primary + sidebar infrastructure (2026-07-21).** See the Completed Tasks entry above for detail.
- [x] **Phase 11.6 — Reader overhaul, part 2 (critical notes and footnotes into the sidebar, 2026-07-21).** `EditionAnnotationsPanel.tsx` now has a Notes tab containing AI-generated critical notes and authorial source footnotes; generated notes reuse exported `findQuoteOffset` through a new pure `matchNoteToBlock()` helper to render dashed in-text markers only when their evidence quote matches exactly one block, while unanchored/ambiguous notes remain sidebar-only and authorial footnotes stay page/sidebar-only because they have no block-level anchor. `EditionReader.tsx` no longer renders critical notes or authorial notes below the page body; `ClaimView`/badge exports are reused in the sidebar so claim evidence, source authority, agreement, and confidence stay visible. Verification: `pnpm --filter web typecheck`, `pnpm --filter web lint`, focused matcher assertion via `pnpm --filter worker exec tsx ../../apps/web/src/app/'(app)'/works/'[workId]'/reader/matchNoteToBlock.test.ts`, and `edition.spec.ts` expanded to 11 tests (all passing against the real local Next/Postgres stack).
- [x] **Phase 11.7 — Relevance/classification fix (Nicomachean Ethics as `prerequisite`, 2026-07-21).** `@ice/ai-adapters` now carries a `citationFrequency` signal into the relationship-classification prompt/heuristic, with a sustained-citation threshold that nudges unresolved but pervasive references toward `prerequisite` without overriding stronger local disagreement/critique cues. `@ice/research` gained two production-found relevance fixes: short distinctive explicit-citation titles can match reference entries even when provider metadata omits the canonical author, and concise canonical work titles surfaced by the explicit-citation lane can use an exact phrase hit in the uploaded document text without accepting longer articles merely about that work. The worker persists the citation-frequency evidence into `edition_relation`/`graph_edge` evidence. Verification: `@ice/ai-adapters` tests/typecheck, `@ice/research` tests/typecheck, worker tests/typecheck, plus production canary run `9c7f6ddc-6473-4b62-9a2f-7480dfa11154` against the private Irwin fixture: `Nicomachean Ethics` published as `prerequisite` with citationFrequency total 119 (`citationMentions` 10, `documentMentions` 109), total cost $0.0756268. Required cleanup completed: Render worker reverted to `ANALYSIS_PIPELINE=v2` via full-list env GET/edit/PUT + deploy `dep-d9fhainavr4c73c8gfag`, confirmed by startup log `pipeline v2`; all canary DB rows/cache/catalog records and the copied fixture object purged.
- [x] **Phase 11.8 — Visualization: nav promotion + relationship-type edge colors (2026-07-21).** The `/graph` route stays unchanged, but the authenticated nav now promotes it as "Visualization" immediately after Dashboard; graph page headings and app shortcut links use the same label. Relationship family metadata (`reference`, `prerequisite`, `influence`, `opposition`, `structural`) now lives in the shared graph `types.ts`; `KnowledgeGraph3D` colors links and directional particles by family, and `GraphView` renders a compact family-level legend rather than 14 individual edge-type entries. Scope stayed UI/read-side only: no new worker pass and no claim that reference↔reference/concept↔concept edges exist yet. Verification: `pnpm --filter web typecheck`, `pnpm --filter web lint`, and `e2e/graph.spec.ts` expanded to 4 tests covering the Visualization nav promotion and edge-family legend.
- [x] **Phase 11.9 — Visualization: merged graph+table layout, new filters (2026-07-21).** `GraphView` no longer hides either representation behind a table/3D toggle: the accessible table and 3D scene now render together from one `filterGraphData()` result, stacked table-first on small viewports and side-by-side on desktop. `buildGraph()` now exposes `credibilityScore` on reference nodes from the existing `research_resource` → `credibility_assessment` join; shared graph filters gained URL-synced `credibilityBand` and `associatedWork` controls. The seeded graph helper now writes the same credibility join shape production uses. Verification: `pnpm --filter web typecheck`, `pnpm --filter web lint`, and `e2e/graph.spec.ts` expanded to 6 tests covering merged layout, credibility-band filtering, and associated-work filtering.
- [x] **Phase 12.1 — Foundation and site-wide UX (2026-07-21).** `@ice/config` now owns independently addressable Phase 12 release flags; future parts default off. A new responsive authenticated shell adds compact page headers, mobile navigation drawer, `Ctrl/Cmd+K` command palette with owned-work search, tooltips, toasts, and route loading/error recovery. Authenticated presentation preferences are Zod-validated and persisted in the existing `users.preferences` JSONB under `workspace`, with local storage only as a pre-hydration paint/synchronization cache; a dedicated table would duplicate an already safe, additive store before Part 2 proves a need. System/light/dark, font size, reading width, focus mode, and script-display values are active globally; future reader term rendering remains gated until Part 4. Baselines: `docs/baselines/phase-12-foundation/landing-desktop.png` and `landing-mobile.png`. Verified: config tests (13), web typecheck/lint/build, and `workspace-shell.spec.ts` against local Next/Postgres. No migration/canary required.
- [x] **Phase 12.2 — Library, identity, and source coverage (2026-07-21, production-enabled).** `learning_resource.normalized_key` remains the canonical DOI → ISBN → canonical URL → title/author/year identity, and the Library now aggregates all roles/recommended works into one source row rather than repeating it. Migration `0020_phase_12_library_identity` adds an indexed, verified document SHA-256 hash; a flagged upload flow offers **Open existing** or **Add as another edition**, preserving a duplicate edition's canonical work identity. The worker calculates the hash only from validated/scanned Storage bytes. Library rows visibly label source type, peer-review state, authority, creator verification, and credibility. Tavily-only public Reddit result metadata is retained as an outbound social source with its search snippet (no Reddit adapter or scraping) and authority E, so D/E sources remain discovery/interpretation material and cannot independently substantiate facts. A shared cumulative reader-level matcher now powers Library, Roadmap, Curriculum, and published-reader annotations; each offers an exact-level facet while retaining universal material. Verified: local migration, roadmap (26), research (220), and curriculum (17) tests; all affected package/web typechecks; web lint/build; and 7 flag-enabled local Playwright Library tests. Production migration and Library identity gates are live; v4 processing confirmed the verified hash path on both the disposable canary and the retained backfill.
- [x] **Phase 12.3 — Pipeline v4 and annotations (2026-07-21, production-complete).** `@ice/config` accepts `ANALYSIS_PIPELINE=v4` while preserving v2/v3 rollback; the worker uses it only with `PHASE_12_PIPELINE_V4_ENABLED=true`. Additive migration `0021_shallow_nova` adds `passage_annotation.helpful_for`/scope, `document_apparatus` (footnote/endnote/bibliography entry/citation block), source-grounded `work_claim`, compact JSONB `work_embedding`, and unjudged `work_relationship_candidate` rows. V4 sends at most eight section-aware chunks of at most 12,000 characters to the existing grounded annotation synthesizer, carries page/section/block scope and a short reader purpose, and deduplicates overlapping anchors by confidence. It uses resolved run metadata title/author, keeps v3's single-batch behavior unchanged, extracts author apparatus deterministically, derives grounded argument/evidence claims, caches a key-gated `text-embedding-3-small` work signal, and stores retrieval candidates without judging a relationship. The Admin page and authenticated `/api/admin/pipeline-v4/backfill-forecast` route expose a no-side-effect forecast. Verified locally: migration `0021`, config (14), research (223), ingestion (32), AI-adapter (25), and targeted worker v4 (2) tests; DB/worker/web typechecks. Production comparison: disposable v3 `8e38b76c` was full/non-degraded with 30 A/B resources, seven annotations, and $0.0518938; v4 `e2b9b20e` was full/non-degraded with 42 A/B resources (44 total), 33 grounded annotations, 13 apparatus rows, seven claims, one embedding, no graph request, and $0.0861008. The separately approved forecast (six chunks, $0.012960 incremental/$0.066563 total) then backfilled the lone retained document as full/non-degraded v4 (`9905dc7b`, $0.0850599); the resumable forecast now has zero eligible documents.
- [x] **Phase 12.4 — Interactive reader (2026-07-21, production-enabled).** Additive migration `0022_purple_black_bolt` adds canonical many-to-many `note_highlight` links (backfilled from the retained legacy pointer) plus suggested/verified `term_variant` and anchored `term_occurrence` records. With `PHASE_12_INTERACTIVE_READER_ENABLED=true`, an edition remains the default reading surface but is explicitly labelled **Processed text**; source-file choices are **Original PDF** or **Original text**, never a purported published edition. The reader adds an outline, persisted page/block position, selected+foundational or exact level matching, type/relationship/provenance filters, previous/next anchored-annotation navigation, independently filterable author apparatus, and a focus layout that leaves only processed text, the annotation rail, and the shell’s always-accessible exit control. Text selection can highlight, begin a linked note, or attach an existing note; original-script/transliteration switching renders only user-approved pairs, with language/direction/accent metadata on processed text and no change to original PDFs. Authenticated ownership checks protect new note-link and term-approval routes; all new schema reads/writes stay dormant unless the reader flag is enabled. Verified locally: migration `0022`, DB/web typechecks, web lint, `git diff --check`, and production web build. A seeded Playwright edition attempt reached the existing onboarding redirect (`/welcome`) before the reader, so it did not provide reader coverage and is recorded as a pre-existing harness block. Production migration and reader gate are live; the retained work now has a full published v4 reader run.
- [x] **Phase 12.5 — Cross-library graph (2026-07-21, production-enabled).** Additive `0023` supplies evidence-hashed directional work judgments and idempotent expansion requests. BM25 + v4 embedding candidates are retrieval-only; only a grounded, cached judgement becomes a typed work edge with confidence, explanation, claim anchors, provenance, and cost telemetry. The global workspace contains every owned work (including isolated ones), canonical reference/concept nodes, search/filters, hover/click evidence, fullscreen/PNG actions, stable layout settings, and the mandatory accessible table. Upload-triggered expansion is bounded to 20 candidates/$0.25; manual expansion performs a read-only estimate, requires explicit confirmation over $1, and cannot start above $5. Verified locally: migration `0023`, research tests (225), DB/worker/web typechecks, web lint/build, and six seeded Playwright graph flows. Production schema, guarded web gate, and automatic worker flag are enabled after the v4 comparison/backfill gates; no graph request or judgement exists yet because the retained Library has one work.
- [x] **Phase 12.6 — Writer mode (2026-07-21, production-enabled).** Additive `0024_phase12_writer_mode` supplies private user-owned projects, documents, bounded immutable revision snapshots, and project-scoped CSL-JSON citations. Writer APIs are flag-gated, authenticated, Zod-validated, and scope every document/revision/citation/export through its owning project, returning 404 across accounts. The responsive workspace has resizable owned-Library sources with processed-reader links, autosave to canonical ProseMirror JSON, document creation, revision restore, MLA parentheticals/Works Cited, and DOI/ISBN/title, BibTeX, RIS, and Library citation ingestion. DOCX is a standards-compliant OOXML ZIP and PDF is a valid text export; both include the generated bibliography. Verified locally: migration `0024`, DB/worker/web typechecks, web lint, all 225 research tests, and two flag-enabled local Chromium flows covering autosave, BibTeX parsing, revision UI, binary DOCX/PDF signatures, and cross-account API denial. Production migration and authenticated Writer gate are live; no user draft, external citation lookup, or export was created during rollout.
- [x] **Phase 12.7 — Production hardening (2026-07-21, production-complete).** Additive `0025_phase12_hardening` supplies fixed-window user/scope rate-limit state, with an intentionally temporary in-process fallback only while old production schemas are still rolling. Writer and upload APIs now rate-limit authenticated callers; graph preview/expansion adds bounded limits plus idempotency keys, while the existing $0.25/$5 cost caps remain authoritative. The worker emits content-free structured graph cost/error events, and all direct/signed upload failure paths use the same web error seam; web/worker Sentry SDK wiring is dormant unless an encrypted DSN is set. Archive and permanent delete now require explicit confirmation; the work trash exposes an explicit undo, and Writer supports project rename, document reorder, archive/restore, and recovery. Verified locally: migration `0025`; observability/worker/web typechecks; web lint/build; 20 production-mode Chromium flows including graph idempotency/rate limiting and Writer archive/restore; axe WCAG 2A/2AA, keyboard, reduced-motion, dark-mode, RTL, desktop/mobile visual coverage; a bounded 40-request public load run (0 failures, 408 ms mean, 913 ms p95); and a Postgres dump/restore drill into a separately named temporary database, followed by table/migration validation and removal. Production migrations `0020`–`0025` were applied transactionally with exact Drizzle ledger rows and verified at ledger 26/14 Phase 12 tables. Production Auth.js credentials and the normal Storage-aware trash/purge flow were exercised with the disposable canary; it was then fully removed. Render is live on v4 with all Phase 12 worker flags, the $1/$5 caps, and automatic graph expansion’s independent 20-candidate/$0.25 gate; Vercel’s authenticated gates are live. No DSN was set and no Sentry event was sent.
- [x] **Phase 13 — Project record, checklist, and operational audit (2026-07-21).** Structured status source, generated standalone checklist, stale-output CI gate, roadmap replacement, tracker workflow, local release-gate coverage, and read-only production audit complete. Phase 14 confirmation received.
- [x] **Phase 14.1–14.3 — Focused Library, shared interactions, and sequential batch upload (2026-07-21).** The server payload now retains all active uploads and per-focus relationship/credibility evidence; the Library defaults to the newest upload (even with no recommendations), supports `?focus=` deep links and all-work switching, anchors its focused work, and orders its default view by relevance, authority/credibility, then title. Added shared warm-academic `.app-control`/reduced-motion-safe `.app-reveal` behavior and converted upload to a sequential private batch queue with progress, per-file errors, and explicit duplicate decisions. Targeted browser verification: 12 Chromium checks pass.
- [x] **Phase 14.4 — Verification and governed closeout (2026-07-21).** Passed workspace typecheck, workspace unit tests, web lint, production build, generated-checklist validation, 12 targeted Chromium checks, and the full 46-test CI-safe Chromium matrix. Checklist, canonical project record, and ignored `CLAUDE.md` mirror are synchronized; commit/push/tag handoff is complete and Phase 15 remains confirmation-gated.
- [x] **Phase 15 — Connected research web closeout confirmed (2026-07-21).** Authorization to begin Phase 16 was received; Phase 15 remains tagged `phase-15-complete` with its provenance and bounded-retrieval record intact.
- [x] **Phase 16.1–16.4 — Reliable processing and Interactive Reader (2026-07-21).** Migration `0027` makes source markers durable and adds the `endnote` structural kind. GROBID/PDF, text/Markdown, worker persistence, and edition assembly preserve body, captions, bibliography/reference, footnotes, and endnotes as distinct page/block-scoped records; structural uncertainty is labelled `structure-limited` instead of being silently flattened. Published edition exposes immutable originals; Interactive reader exposes continuous processed prose, an outline, and source-aware position navigation. Margin/inline annotation expansion, the sidebar’s index/detail behavior, the linked Footnotes apparatus, and source/confidence/provenance evidence are all released. Passed local ingestion/unit/type/lint/build checks and the 11-flow reader suite; production migration ledger is 28, Vercel/Render are live, and the single $0.0845135 PDF canary verified full structure, anchors, footnote/body separation, immutable-source reader response, and complete cleanup. Tag `phase-16-complete`; Phase 17 remains confirmation-gated.
- [x] **Phase 17 — Citation Completeness, Library Integrity, and Research-Web Repair (2026-07-21).** Local acceptance proved anchored citation/Library integrity and D/E supplementary-source/Visualization behavior. The later owner-authorized release applied `0028` as production ledger 29 and deployed this repair with Phase 18; the manual host-level Phase 18 context replacement remains accurately recorded.
- [x] **Phase 18 — Library-grounded Socratic RAG chat (2026-07-21).** Production release complete: migrations `0028`/`0029` are ledgered through 30; Vercel and Render run `93e4146`; web/provider and worker/embedding gates are enabled; 338 eligible chunks and 64 capped embeddings are backfilled; a cited live OpenAI answer passed within cost/latency caps; and public/authenticated RAG routing was exercised with disposable artifacts removed. Post-release `03558aa` makes Ask Library directly discoverable from authenticated navigation at `/ask-library` while retaining its Reader shortcut. The provider licensing boundary and D/E supplementary-source treatment remain intact.
- **2026-07-21 (Phase 18 production release and credential remediation).** Under explicit owner authorization, applied Drizzle migrations `0028` and `0029` to Supabase production in ordered transactions with the exact local hashes/timestamps, verified ledger 30 and all citation/RAG tables/cascades, pushed `93e4146`, and confirmed Vercel Ready (`dpl_8VHHpCPBmNxcAZfvXnriJpT4Lvpa`) plus Render live (`dep-d9g1i3sm0tmc73de53dg`). Enabled Vercel `PHASE_18_RAG_ENABLED` and `PHASE_18_RAG_PROVIDER_ENABLED`, Render `PHASE_18_RAG_ENABLED` and `PHASE_18_RAG_EMBEDDINGS_ENABLED`, and the encrypted Vercel OpenAI key. Backfilled the two published documents through the same eligible-source logic: 338 chunks, 64 embeddings, $0.000259. A live OpenAI answer used two real reader anchors in 3,147 ms at $0.00067615; its temporary conversation was deleted. The production alias served 200; the enabled unauthenticated endpoint returned 401; and a disposable verified Auth.js account logged in and created a RAG conversation (201) before its account/session artifacts were removed. Preflight exposed stale production database credentials in both deployment environments; rotated the Supabase primary password and updated Vercel/Render encrypted pooler URLs without displaying or recording secret values.
- **2026-07-21 (Phase 18 Ask Library discoverability correction).** A live review found that the released RAG implementation was reachable only from a ready work's Reader toolbar, not from the authenticated workspace navigation, Library, or Dashboard. This was a placement defect, not an API, data, or feature-gate failure. Under the existing owner production authorization, shipped `03558aa`: the authenticated shell now shows **Ask Library**, `/ask-library` renders the inline owner-scoped chat without a work hint, and the Reader retains its context-aware drawer. The full local two-flow RAG Playwright spec passed (new navigation/page flow plus existing cited Reader answer/not-found flow); Vercel is Ready and Render deploy `dep-d9g1s7740ujc73fggh30` is live. A disposable production account logged in, reached `/ask-library`, and confirmed the navigation and chat markup before deletion; a direct count verified zero release-navigation test users remain.
- [x] Embedding-backed candidate retrieval is live in v4, and Phase 18 stores bounded eligible-source embeddings under its separate worker gate. RAG answer ranking intentionally retains its evidence-required lexical baseline; this is a design boundary, not an unreleased feature.
- [x] **GitHub↔Vercel Git integration connected (2026-07-21).** The Vercel GitHub App needed a one-time browser authorization on the private repo (the CLI's `vercel git connect` returned a 400 until the user granted access via github.com/apps/vercel — no CLI/API path could do this step, same category of gotcha as Render's own GitHub-connection requirement documented below). Once authorized, `vercel git connect` succeeded ("already connected"). Every push to `main` now auto-deploys to production — matching how the Render worker has always deployed — so the manual `vercel --prod` step from every prior phase checkpoint is no longer needed. Verified end-to-end, not just via the CLI's own success message: pushing this very doc update produced a new deployment in `vercel deployments list` sourced from that exact commit, with no `vercel --prod` run — see the note this replaces below in Deploy: for the confirmed commit SHA.
- [x] EPUB and scanned/OCR PDF ingestion: DRM-free EPUB validation/parsing, page-bounded PDF.js OCR with Tesseract, and the local GROBID extraction/fallback path are implemented in Phase 8.
- [x] Reader-owned edition reprocessing: a fresh v2 run is queued from the work page and the previous published run remains intact until the replacement succeeds.


The condensed one-line resolved rows themselves (everything `[x]`-checked through 2026-07-23) were also moved here verbatim from `docs/PROJECT-LOG.md`'s "Remaining Tasks" section on 2026-07-23, in the exact order they appeared there:

- [x] Fixed the invalid-password login path (Phase 8.1).
- [x] Clean-slate production purge + private Irwin eval fixture (2026-07-19).
- [x] Irwin gold-eval steering package (2026-07-19) — `docs/eval/irwin-vice-and-reason/`.
- [x] Phase 8 relevance closeout — gate built and calibrated (2026-07-19); migration `0013`, 36 tests.
- [x] Lane-specific discovery + research-review UI (2026-07-19).
- [x] Live gold-eval canary run (2026-07-19) — displayed precision 100% (4/4).
- [x] Relevance recall fixes + canary run 4 (2026-07-19) — 8/8 accepted, precision 100%, recall doubled.
- [x] Note-style citation extraction built (2026-07-19) — 0 → 9 citations on the Irwin fixture.
- [x] Render deploy gap resolved (2026-07-20) — root cause was a GitHub outage, not a lapsed connection.
- [x] Explicit-citation recall largely solved (2026-07-20, canary 10) — one lookup per cited work; ~88% resolve rate.
- [x] Work-level identity built (2026-07-20) — `workIdentity.ts` + migration `0014`, 18 tests.
- [x] Work grouping shipped to the reader (2026-07-20) — 21 records → 12 works in production.
- [x] Superseded note (resolved by Phase 16): footnote/bibliography extraction.
- [x] Phase 8 acceptance gates met (2026-07-20) — 13 production canaries; 10-PDF load test 10/10 published, $0.48 total.
- [x] Tagged `phase-8-complete` (2026-07-20).
- [x] Phase 9.1–9.7 (2026-07-20) — see condensed Completed Tasks entries above / full detail in `docs/COMPLETED-TASKS-ARCHIVE.md`.
- [x] Regenerated the two Render-only secrets lost in the 9.3 closeout incident (2026-07-20) — `GROBID_SA_KEY`, `GOOGLE_BOOKS_API_KEY`.
- [x] Phase 9 (Interactive Learning Workspace) — complete, tagged `phase-9-complete`, plan §34.
- [x] Phase 10 (Workspace Depth & Adaptivity Completion) — complete, tagged `phase-10-complete`, plan §35.
- [x] Phase 11.1–11.9 — see condensed Completed Tasks entries above / full detail in `docs/COMPLETED-TASKS-ARCHIVE.md` (11.1–11.5) and `docs/CHANGELOG.md` (11.6–11.9).
- [x] Phase 12.1 — Foundation and site-wide UX (2026-07-21) — `@ice/config` release-flag contract, responsive shell, presentation preferences.
- [x] Phase 12.2 — Library, identity, and source coverage (2026-07-21, production-enabled) — migration `0020`, verified-hash duplicate-edition flow.
- [x] Phase 12.3 — Pipeline v4 and annotations (2026-07-21, production-complete) — migration `0021`; v4 comparison canary + approved backfill.
- [x] Phase 12.4 — Interactive reader (2026-07-21, production-enabled) — migration `0022`; outline/position/filters, note-highlight links, term variants.
- [x] Phase 12.5 — Cross-library graph (2026-07-21, production-enabled) — migration `0023`; evidence-hashed work judgments, bounded expansion.
- [x] Phase 12.6 — Writer mode (2026-07-21, production-enabled) — migration `0024`; ProseMirror projects, MLA citations, DOCX/PDF export.
- [x] Phase 12.7 — Production hardening (2026-07-21, production-complete) — migration `0025`; rate limits, cost/error telemetry, dump/restore drill.
- [x] Phase 13 — Project record, checklist, and operational audit (2026-07-21).
- [x] Phase 14.1–14.3 — Focused Library, shared interactions, and sequential batch upload (2026-07-21).
- [x] Phase 14.4 — Verification and governed closeout (2026-07-21).
- [x] Phase 15 — Connected research web closeout confirmed (2026-07-21).
- [x] Phase 16.1–16.4 — Reliable processing and Interactive Reader (2026-07-21) — migration `0027`; production ledger 28, canary $0.0845135.
- [x] Phase 17 — Citation Completeness, Library Integrity, and Research-Web Repair (2026-07-21) — production ledger 29 (`0028`).
- [x] Phase 18 — Library-grounded Socratic RAG chat (2026-07-21) — migrations `0028`/`0029` through ledger 30; `/ask-library` discoverability fix.
- [x] Phase 19.1 (partial) — Render worker crash (`unsupported Unicode escape sequence`) diagnosed and repaired (2026-07-22) — `packages/ingestion/src/sanitizeText.ts`, worker crash observability.
- [x] Phase 19.P — pre-flight everything-works gate (2026-07-22) — typecheck/lint/tests/build/CI/production health all verified clean before continuing the audit.
- [x] Phase 19.C — generated checklist rebuilt to schemaVersion 2 (2026-07-22) — model assignments, program progress, defect summary.
- [x] Phase 19.I/19.J — interaction inventory and journey completion (2026-07-22) — 4 real defects found and fixed (D-19-21/25/29/34); contextual Ask Library and full signup+verification E2E remain explicitly deferred/noted.
- [x] Phase 19.D — authorized production cleanup (2026-07-22) — 9 orphaned Storage objects, 7 orphaned catalog rows, and the test-reader account deleted; anti-joins recheck to zero. VoiceOver remains explicitly out of scope (agent has no macOS Accessibility API).
- [x] **Phase 19.X — closeout (2026-07-22):** tag `phase-19-complete` created and pushed; prioritized Phase 20–24 order recorded; Phase 20 begins.
- [x] **Phase 20 wave 1 (2026-07-22):** 20.1 Library search, 20.3 permanent-delete lifecycle (migration `0030` applied local + production), 20.5 reprocess reliability — complete locally, 11 register findings fixed, commits `c728aa2`/`527979f`/`967db68`. See Current Implementation Status.
- [x] **Phase 20 wave 2 (2026-07-22):** 20.2, 20.4, 20.6 complete and committed (see Current Implementation Status); migration `0031` applied local + production (ledger 32).
- [x] **Wave 3 (2026-07-22):** 20.7, 21.1 (+D-21-1/10), D-21-6, 22.1 (landing pixel-proof 3x via Docker; landing-contract now a CI gate), 22.5 (+D-22-8/9), and the stale work-status test fix — all complete, verified, committed (3ba321e/3b8374d/74f4aa2/7b34c57/546820e/374a97a), pushed.
- [x] **Wave 7 (2026-07-23):** 22.8 Roadmap visualizer UI (commit 7f55bd8) + landing inference-basis language migration + Docker baselines regenerated (commit f3eb213, D-22-23 fixed). Phase 22 now at 7/9; only 22.3/22.6 remain.
- [x] **Local test-account cleanup (2026-07-22, owner-requested):** `owner-test@palimnote.local` plus 76 stale E2E accounts, their 129 works/23 documents, and all 23 path-scoped Storage objects deleted and re-verified gone; live concurrent test users spared via a creation-time cutoff.
- [x] Embedding-backed candidate retrieval is live in v4, and Phase 18 stores bounded eligible-source embeddings under its separate worker gate. RAG answer ranking intentionally retains its evidence-required lexical baseline; this is a design boundary, not an unreleased feature.
- [x] GitHub↔Vercel Git integration connected (2026-07-21) — every push to `main` now auto-deploys to production.
- [x] EPUB and scanned/OCR PDF ingestion: DRM-free EPUB validation/parsing, page-bounded PDF.js OCR with Tesseract, and the local GROBID extraction/fallback path are implemented in Phase 8.
- [x] Reader-owned edition reprocessing: a fresh v2 run is queued from the work page and the previous published run remains intact until the replacement succeeds.


## Current Implementation Status — Phases 12–18 full text (moved 2026-07-22)

Verbatim "Current Implementation Status" paragraphs for Phases 12–18, moved out of `docs/PROJECT-LOG.md` on 2026-07-22 to keep that file under the ~150,000-character tooling limit. The project log keeps condensed summaries; this is the full historical text.

**Phase 12 — Production Research Workspace — complete (2026-07-21), plan of record `docs/architecture/plan.md` §37.** All seven parts are now production-released: the cross-library graph has durable evidence-hashed work-pair judgments, BM25 plus `text-embedding-3-small` candidate retrieval, strict automatic 20-candidate/$0.25 caps, manual preflight/confirmation above $1 with a $5 hard stop, cost logs, and an accessible responsive workspace with search, fullscreen/PNG export, and claim anchors. Writer mode has private projects/documents, canonical ProseMirror JSON, bounded revision recovery, owned-Library sources, CSL-JSON citation ingestion, MLA parentheticals/Works Cited, and valid binary DOCX/PDF exports. Hardening provides shared database rate limits, idempotency, upload safeguards, structured cost/error telemetry, dormant Sentry wiring, destructive-action confirmations, and automated accessibility, visual, load, and restore-drill coverage. Additive migrations `0020`–`0025` are applied in production (Drizzle ledger 26; all 14 Phase 12 tables verified). Render is live on v4 with Library identity, automatic cross-library expansion, and explicit $1 normal/$5 hard research caps; Vercel deployment `dpl_4ZLArqPV3pzXK8vRLPeSuhhJRheM` is Ready with Library, reader, graph, Writer, and v4 web gates enabled. The same-corpus v3/v4 canary met full-structure, non-degradation, source-authority, output, and cap gates; the separately approved one-work backfill then published full/non-degraded v4 at $0.0850599 and zero eligible v2/v3 work remains. The disposable canary, its queue rows, copied Storage object, temporary credentials, and session artifacts were verified removed. No graph request exists because there is one retained work; future qualifying multi-work uploads are bounded automatically by 20 candidates/$0.25. Foundation retains the shared responsive shell and validated presentation preferences; source coverage retains Tavily/YouTube/Mastodon/Bluesky configuration with no direct Reddit/Blogger adapter.

**Phase 13 — Project Record, Checklist, and Operational Audit — complete (2026-07-21), tagged `phase-13-complete`.** `docs/project-status.json` is now the tracked status source for phase/sub-phase state, confirmation gates, evidence, next action, and generated totals; `progress-checklist.html` is generated by `pnpm generate:project-status`, verified by `pnpm check:project-status`, and checked in CI. The root `CLAUDE.md` is an ignored byte-for-byte mirror of this file. The roadmap in plan §23/§31 now replaces the obsolete separate-site proposal with Phases 13–17. Audit evidence: 13 targeted config/worker tests passed; 27 seeded Chromium flows passed for Library, reader, Visualization/graph guardrails, and Writer; a stale reader-heading assertion was corrected to the released UI text. Production was inspected read-only: Render's current worker startup reports pipeline v4 and a v4 analysis run; Vercel's latest production deployment is Ready; Supabase reports one active work, one full published v4 run, zero graph-expansion requests, and zero Writer projects. Classification: v4 processing and Library are **operational**; Interactive Reader, Visualization, and Writer are **release-gated** because their local flagged behavior is verified but the retained production data does not exercise reader mutations, multi-work graph expansion, or Writer authoring. No surface is marked incomplete. Phase 14 confirmation was received on 2026-07-21.

**Phase 14 — Library Focus, Interface, Motion, and Multi-work Upload — complete (2026-07-21), tagged `phase-14-complete`; awaiting confirmation before Phase 15.** The Library now receives every active upload (not only the ones that already have recommendations) and defaults its Focus selector to the newest upload, including a deliberate empty-focused-library state. `getLibrary()` carries per-focus resource-role relevance plus work-scoped credibility evidence, so the default **Most relevant and credible** order is relationship relevance, authority/credibility, then stable title; title, recency, and credibility remain explicit alternatives. Focus is shown as the work anchor, supports `?focus=<work-id>` deep links, and the permanent Library note distinguishes privately indexed uploads from open-access full text and metadata-only source records. Shared `.app-control` and progressive `.app-reveal` patterns add responsive hierarchy, hover/focus feedback, and one-time scroll entrance without motion for readers who request reduced motion; 3D remains Visualization-only. Upload is now a multi-file drop/picker queue that privately processes files sequentially as separate works, reports each file's status/progress/error, pauses only for an explicit duplicate skip/add-edition decision, then continues the remaining files. Verification: workspace typecheck, workspace unit tests, web lint, production build, generated-status validation, 12 targeted Chromium checks, and the complete 46-test CI-safe Chromium matrix all passed locally.

**Phase 15 — Visualization as a Connected Research Web — complete (2026-07-21), tagged `phase-15-complete`; Phase 16 authorization was received 2026-07-21.** Migration `0026_phase15_research_web` adds a separate cascading `research_resource_content` record (text, hash, source URL, normalized license, exact license evidence, retrieval outcome/error) so source text is never confused with provider metadata. It was applied and verified locally and in production before code rollout; production Drizzle ledger is now 27. The worker accepts source text only when provider metadata includes an explicit approved license (an `is_oa` flag alone is insufficient), retrieves bounded HTML/plain text only, caps this network-only work at 12 sources per run, and retains binary, inaccessible, failed, and unlicensed sources as visibly non-indexed records. This does not create any AI call or raise the existing $1 normal/$5 hard budget.

The Visualization now projects each owner’s published uploaded works, catalogue references, peer-reviewed and online sources, concepts, people, and work-scoped sections. Source nodes and links carry provider/run/access/retrieval provenance; deterministic shared-work review/edition/translation relations are persisted as source-to-source links instead of similarity guesses. Users can select multiple uploaded works with shareable `pinnedWork` URL parameters; pins remain in the type/credibility/search-filtered 3D graph and accessible table. The force-directed web is type-coloured with directional relationship-family links, hover-neighbor emphasis, cursor/camera feedback, and restrained particles; particle/camera effects simplify for reduced motion, hidden tabs, and large graphs. The bounded, scrollable in-frame inspector is shared by graph nodes and keyboard-operable table rows, and states whether a source is indexed open access, available but not indexed, retrieval-failed, or metadata-only. Verification: workspace typecheck, all workspace unit tests (including 3 explicit-license retrieval tests), web lint, production build, 10 focused Chromium Visualization checks (provenance, source-to-source link, pinning/deep-link reload, graph/table inspector parity, keyboard, reduced motion), and 2 graph-route ownership checks passed locally. The `4bfdd9b` code rollout reached Vercel Ready (`dpl_7WV5K8bmjyk9JsXyvsuFxxGhZSzu`) and Render live (`dep-d9fuvmpoagis73bu4tcg`) before the final documentation-only sync; the production alias continues to serve the same code. Read-only smoke checks confirm `/graph` redirects anonymously to login and `/api/graph` remains 401. See `docs/handoffs/phase-15.md`; `research/` and `research.zip` remain untouched and untracked.

**Phase 17 — Citation Completeness, Library Integrity, and Research-Web Repair — complete (2026-07-21), tagged `phase-17-complete`.** Reconciliation confirms that the structured tracker and `main` already record the Phase 16 delivery (`e3c1f4f`/`5ff44b8`); the former Phase 17 RAG work is Phase 18. `@ice/phase-lifecycle` reads `CLAUDE.md`, `docs/PROJECT-LOG.md`, and `docs/project-status.json` in order for every task, verifies before closeout, and preflights a real host context-replacement capability before writing a tracker advance. Its original session failed closed without such an adapter. Phase 17 adds structurally anchored bibliography/footnote/endnote/inline citations, unconditional immediate Library projection plus queued resolution, D/E supplementary public-source coverage, and canonical topic-connected Visualization data/UI. The deterministic *Vice and Reason* fixture passes with 22 distinct resolved targets, 23 anchored Library items including one unresolved reference, and 22 graph targets; mocked worker public-source and 13 focused Chromium graph checks also pass. The later owner-authorized Phase 18 production release applied `0028` as production Drizzle ledger 29 and deployed this repair alongside RAG. The user explicitly created this fresh session from `docs/handoffs/phase-17-to-18.md`, so Phase 17 is closed under a **manual host-level context replacement**; no automatic adapter action or simulated compaction is claimed. `research/` and `research.zip` remain ignored and untouched.

**Phase 18 — Library-grounded Socratic RAG chat — production release complete (2026-07-21), tagged `phase-18-complete`.** Additive migration `0029_real_hammerhead` creates owner-scoped `rag_chunk` rows (uploaded body or explicitly licensed `open_access_indexed` content only), persistent conversations/messages, and cascading answer-citation links; it is applied in production as Drizzle ledger 30 after Phase 17's `0028` at ledger 29. Chunk anchors are real reader block/page offsets or license-evidenced source URLs; each chunk has a SHA-256 hash, optional vector, and deterministic deletion propagation from document/run/content deletion. Reprocessing deletes stale document chunks before inserting the newly published run. The authenticated API persists each user question and assistant response before emitting server-sent answer events; retrieval is SQL-scoped by owner before lexical ranking, substantial answers require a retrieved citation, and no-evidence questions receive an explicit not-found response. The reader retains its contextual **Ask Library** desktop side panel/mobile sheet, and the authenticated shell now also exposes **Ask Library** navigation to the dedicated inline `/ask-library` page; citations jump to stable processed-text `#block-…` anchors. Dialogue behavior is a locally authored provider prompt informed only by the Socratic method: no SocraticLM data, weights, or weight-diff artifacts are imported or used. The owner explicitly authorized production release: Vercel deployment `dpl_8VHHpCPBmNxcAZfvXnriJpT4Lvpa` is Ready and Render deploy `dep-d9g1i3sm0tmc73de53dg` is live at `93e4146`; web RAG/live-provider and worker RAG/automatic-embedding gates are enabled. The production backfill wrote 338 eligible chunks for two published documents and 64 bounded embeddings for $0.000259. A paid live OpenAI answer produced two reader-anchor citations in 3,147 ms for $0.00067615, and its temporary conversation was removed. The public alias returns 200, the enabled anonymous RAG route returns 401 rather than 404, and a disposable verified user completed Auth.js login plus a 201 RAG conversation creation before cleanup. The release preflight also found stale deployment database URLs; the Supabase primary password was rotated and both Vercel and Render encrypted pooled URLs were updated without recording any secret value.


## Phase 19 detailed narrative paragraphs (moved from PROJECT-LOG 2026-07-22)

**Phase 19.P — pre-flight everything-works gate (2026-07-22).** Before continuing the audit, re-verified the whole stack: `pnpm -r typecheck`, `pnpm -r lint`, `pnpm -r test` (all workspace Vitest suites), and `pnpm --filter web build` all pass; `gh run list` shows CI green on the last 5 pushes to `main`; the live Render worker (instance `srv-d9dgiamrnols73ccpa10-ksj74`, commit `b9b5bcc`, `pipeline v4`) and the latest Vercel production deployment are both healthy and current — the earlier per-deploy `SIGTERM` log lines are confirmed to be each prior instance's delayed teardown, not a crash, consistent with the documented pattern; the Supabase migration ledger is confirmed at 30 via `supabase db query --linked`; and the full CI-safe Playwright subset (11 spec files, 60 tests) passes locally against the local Postgres/GROBID stack. No errors found; nothing needed fixing before proceeding. (Also relinked Homebrew's active `node` from a stray 26.5.0 back to the pinned 24.18.0 — `.node-version`/`engines` were correct, but the global symlink had drifted.)

**Phase 19.I/19.J — interaction inventory and journey completion (2026-07-22).** Closed every remaining `pending` row from the interaction inventory via 4 parallel Sonnet subagents covering distinct surfaces (Shell preferences + auth journeys; Writer + Library; Work-status + upload journeys; Reader), each following the established audit-before-edit discipline (reproduce, red-first regression, smallest correct fix, re-verify). Two of the four subagents ended their own turns with false "still waiting on a background process" claims despite the harness reporting them `completed` with no live children and `jobs`/`ps aux` on the actual machine showing nothing running — rather than trust their self-reports, every change was independently re-verified from scratch: `git diff` review of every touched file, a full workspace typecheck (clean), and a combined 89-test regression across every spec file touched this session (`auth`, `workspace-shell`, `security`, `edition`, `writer`, `library`, `work-status`, `landing`, `onboarding`, `diagnostic`, `curriculum`, `graph`, `trash`, `upload`), which passed 89/89. Four real defects were found and fixed: **D-19-21** (the Interactive Reader's content column hardcoded a 72ch width, so the reading-width preference had no effect there at all — fixed via a CSS custom property with a 72ch fallback); **D-19-25** (Writer's multi-document reorder persisted correctly, but a `useEffect` keyed on the `active` document's object reference — not its stable id — re-fired on every reorder and its own `setStatus("Saved")` raced with the reorder's real persistence, letting the UI claim "Saved" before the write landed; reproduced 3/12 times in a stress test, 0/12 after keying the effect on the stable id instead); **D-19-29** (a `failed` document had zero recovery action anywhere in the UI — the failed-state branch rendered only an error message; added a "Retry processing" button reusing the existing reprocess handler); **D-19-34** (the Reader's footnote/original-note popup had no keyboard-dialog semantics at all — no role, no initial focus, no Escape, no trigger-focus restoration — brought up to the same standard already established by D-19-18/19/20). Also closed this pass: a comprehensive password-reset E2E suite (request confirmation, valid-token round trip with old-password invalidation, invalid-or-expired-token handling) and desktop-logout verification in `auth.spec.ts`/`workspace-shell.spec.ts`; confirmation that duplicate-upload-decision and processing-failure+retry journeys were already/now covered; confirmation that a metadata-only Library item genuinely has no source-text-upload affordance yet (expected — this is exactly what Phase 20.4 will add, not a defect); and 5 more routes added to the `security.spec.ts` IDOR matrix (with `/reprocess` explicitly excluded and reasoned about, not silently skipped, since it 409s on ownership-independent grounds under the legacy pipeline). Two gaps are recorded as open rather than silently dropped: **contextual Ask Library** is deferred to Phase 22.5's global-sidebar rebuild rather than tested in isolation now, and a fully-scripted **signup-through-email-verification** E2E remains unbuilt (the flow was manually verified end-to-end in Phase 1 and hasn't changed since, but no automated spec drives the real verification-link step). The footnote-modal and non-empty-split-view fixes are typecheck/lint-clean and follow an already-proven pattern, but confirming them against a live, completed upload was blocked by the pre-existing, already-documented D-19-6 finding: 2 of `reader.spec.ts`'s 5 tests time out waiting for the real local v2-pipeline upload to finish (multi-minute, live-network-bound) before ever reaching the new code — not a regression from this session, and the other 3 tests in that file pass.

**Phase 19.D — authorized production cleanup (2026-07-22).** Under the owner's standing 2026-07-22 authorization (recorded below), executed the cleanup D-19-11 had left pending owner action: deleted 9 confirmed-orphaned `documents`-bucket Storage objects (one more had appeared since D-19-11's original count of 8 — an `e2e-reader-*.txt` file from a subsequent local test run; re-verified as genuinely orphaned by anti-join immediately before deletion, not assumed) and 7 orphaned `googlebooks` `bibliographic_record` rows (full anti-join across every column that can reference `bibliographic_record`: `annotation.target_bib_id`, `citation.resolved_bib_id`, `learning_resource.bib_record_id`, `reading_record.bib_id`, `research_resource.bib_record_id`, `roadmap_override.bib_id`, `understanding_rating.bib_id`, and `graph_edge.source_id`/`target_id`). Also deleted the `test-reader@interactive-critical-edition.test` acceptance account along with its 3 owned works/documents, their Storage objects, and any queued jobs (none were queued). The Storage deletions used the Supabase Storage REST API directly with the project's own revealed secret API key, after discovering that `supabase storage rm --linked --experimental` silently no-ops (returns an empty `deleted` array with no error) against this project's new-format `sb_secret_...` API key — a CLI limitation now on record rather than a false "success." Both anti-joins, plus a separate check for orphaned page-image objects, were rerun immediately after deletion and confirmed zero.

The following Phase 19 / Phases 19–24-program paragraphs were moved here verbatim from `docs/PROJECT-LOG.md`'s "Current Implementation Status" section on 2026-07-23:

**Phase 19 — Product-Wide Baseline, Contract Capture, and Defect Audit — complete (2026-07-22), tagged `phase-19-complete`.** Plan of record: a governed, owner-provided Phases 19–23 program (audit-before-edit discipline, a protected landing-page visual contract for Reader/Annotations/Roadmap, and a mandatory defect register) covering Library/lifecycle repair, Visualization repair, Reader/Annotations/Roadmap/RAG-sidebar parity, and final hardening/closeout — adopted 2026-07-22 as part of a new owner-approved Phases 19–24 completion program (see the "Phases 19–24 Completion Program" section below for the added Phase 24, per-sub-phase model assignments, and standing authorizations). Phase 19's mandatory first actions were completed: Render's last 20 worker deploys were checked and show no unresolved build/boot failure; one confirmed runtime crash (`unsupported Unicode escape sequence` inside `pg-pool`, triggered via a malformed-Unicode text fragment reaching a `pg-boss` query, 2026-07-20T04:57:20Z) was root-caused, fixed at the ingestion boundary (`packages/ingestion/src/sanitizeText.ts`, wired into `parseDocument()` so every parser — PDF/GROBID, EPUB, text/Markdown — is covered), and regression-tested (5 new Vitest cases; all 43 `@ice/ingestion` tests and both affected packages' typechecks pass). The worker also gained `uncaughtException`/`unhandledRejection` observability routed through `reportError` so a future crash of this shape is diagnosable from structured logs/Sentry rather than requiring manual log archaeology. Full findings: `docs/incidents/render-server-failures-phase-19.md`; standing procedure: `docs/runbooks/render-worker-failure-response.md`. The plan's two mandated frontend tools were checked live rather than assumed: Stitch MCP's `list_projects`/`list_design_systems` both fail with `Incompatible auth server: does not support dynamic client registration` (non-functional as configured), and Uncodixify is not a resolvable plugin in this environment (no match in the tool registry, no plugin-management path). Both are documented with their concrete, already-available substitutes (repository-derived design tokens + Playwright screenshot/axe/reduced-motion coverage in place of Stitch/Uncodixify) in `docs/audits/phase-19-frontend-tooling.md`, per the plan's own fallback instruction rather than skipped silently. Verifying the push also surfaced a genuine, pre-existing CI failure on `main` (not caused by this phase's own commit): `e2e/workspace-shell.spec.ts` failed deterministically because `getByLabel('Theme')` resolved to two elements — the Preferences panel's `<select>` and the header's quick light/dark toggle, whose `aria-label="Color theme"` also matches "Theme" under Playwright's (and assistive technology's) substring label matching. Fixed by renaming the quick toggle's label to `"Quick light or dark switch"`, which disambiguates without touching the control the test (and real screen-reader users) actually target; this doubles as a genuine accessibility fix, not just a test fix. Logged as `D-19-1` in the new `docs/audits/phase-19-product-audit.md` defect register. Verified locally against the real dev Postgres/web stack: the previously-failing spec now passes, and the full CI-safe E2E subset (53 tests, 1 skipped) passes clean.

The landing-page visual contract (§19.4) is now frozen: `apps/web/e2e/landing-contract.spec.ts` adds 4 Playwright `toHaveScreenshot()` assertions (Reader/Annotations and Roadmap showcases × desktop/mobile) against checked-in Linux baselines, generated through the official `mcr.microsoft.com/playwright:v1.61.1-noble` Docker image rather than this machine's native macOS screenshots — Playwright's snapshot filenames are platform-suffixed, and a `-darwin` baseline would be silently ignored by CI's Ubuntu runner. `docs/baselines/phase-19/` holds the human-readable PNG set plus `landing-product-contract.md`, which documents the exact labels/hierarchy/tokens and records the one structural fact worth knowing: the landing page has no separate "Reader" section distinct from "Annotations" — `ReaderShowcase()` depicts both in one combined section, so the plan's `landing-reader-*`/`landing-annotations-*` file pairs are intentionally identical copies of that one section, not two different captures. Not yet wired into the CI-safe spec list — deferred until it becomes a running gate in Phase 22, per the plan's own sequencing.

`docs/audits/phase-19-system-inventory.md` now catalogs all 20 page routes and 55 API routes with their actual auth mechanism (confirmed Writer/RAG routes wrap `getApiUserId()` via `requireWriterApiUser()`/`requireRagApiUser()` rather than lacking owner scoping, correcting a naive first grep), plus the current 23 E2E specs split into 11 CI-safe and 12 manual/non-CI checks. CI on `main` is green again as of the D-19-1 fix (verified via `gh run list`).

Backend/data audit (§19.7) findings, each with full narrative in `docs/CHANGELOG.md` and the defect register: the undocumented ~3h42m production pg-boss SSL crash-loop after the Phase 18 DB-password rotation was root-caused, already fixed (`buildPgBossConfig()`), confirmed recovered via live `render logs`, and backfilled as R-19-2/D-19-3; orphaned pg-boss jobs surviving document hard-deletes were fixed with `cancelQueuedJobsForDocuments()` wired into `purgeWork()` and `deleteTestUser()` (D-19-2), and that fix's own CI regression on fresh schema-less databases was self-caught and repaired with a `42P01` guard (D-19-4). A later pass verified production ledger parity (all 30 migration timestamps), zero dangling graph endpoints, zero duplicate document paths, and zero missing Storage objects — while recording, not deleting, eight unreferenced `documents` objects (3,104,281 B) and seven unreferenced Google Books rows as D-19-11 pending owner authorization. A signed-upload quota bypass was fixed locally as D-19-10: staging rows now reserve quota immediately, completion reads the authoritative Storage object size and removes mismatches, and objectless reservations are released (three real-Storage regressions pass). Local E2E now loads gitignored env files when present without changing CI (D-19-9), and a Next streaming-template test flake was repaired by scoping `curriculum.spec.ts` lookups to `#main-content` (D-19-12; 20/20 repeat with retries disabled).

The literal per-control interaction inventory (§19.5–19.6, `docs/audits/phase-19-interaction-inventory.md`) is underway with red-regression-first repairs, all re-run green in their focused suites: Writer's pointer-only sidebar resize is now a value-bearing keyboard-operable separator (D-19-13); the command palette and mobile navigation overlay are focus-contained dialogs that Escape-close back to their triggers (D-19-14/D-19-15); Library's reading-status filter exposes a labelled group with `aria-pressed` from the real filter state, plus a CI-safe regression through status/relationship/source-type/sort changes (D-19-16); the work-scoped graph action is accessibly named "Visualization for [work title]" to disambiguate it from global Visualization, with a ready-work journey regression covering Roadmap/Concept check/Curriculum/work graph/Reader and a cancelled Move-to-trash (D-19-17); the Reader Split-view chooser gained `aria-expanded`/`aria-controls`, a labelled group, and Escape-with-focus-restore (D-19-18, empty-chooser path only); and Workspace preferences is now a labelled non-modal dialog with trigger relationship, initial close-button focus, and Escape/close focus restoration (D-19-19). The inventory is deliberately partial — unexercised control families, journeys, and fixture-dependent states (metadata confirm, reprocess, processing/failed, trashed-work Undo, non-empty split selection) are marked pending, not passed; the browser-runtime binding is unavailable this session, so evidence is the repository's real local Playwright harness, and manual VoiceOver remains explicitly open.

**Owner-approved Phases 19–24 completion plan (2026-07-22).** The owner reviewed this file and `palimnote_phases_19_23_plan_revised.md`, then approved a plan to finish the project: the revised plan is adopted verbatim for Phases 19–23 (including its already-documented Stitch MCP/Uncodixify substitutions), a new **Phase 24 (Final End-to-End Verification and Program Sign-off)** is added at the owner's explicit request, and every phase/sub-phase is assigned a cost-efficient model (Haiku 4.5 for mechanical work, Sonnet 5 as the implementation default, Opus 4.8 reserved for high-stakes/hard-to-reverse work — permanent delete, canonical-identity merges, the graph data contract, security hardening, production rollout). Standing owner authorizations recorded here so future sessions don't need to re-ask: full autonomy to execute the plan; explicit approval to delete D-19-11's 8 unreferenced Storage objects/7 orphaned catalog rows and the `test-reader@interactive-critical-edition.test` acceptance account (Phase 19.D); explicit approval to run paid production canaries autonomously at phase gates within the existing $1 normal/$5 hard cost caps, always cleaned up and cost-logged; and a hard prohibition on any new subscription, signup, or payment-setting change anywhere. `docs/project-status.json` is now schemaVersion 2 with a `program` block (Phases 19–24), per-sub-phase `model` fields, and a `defects` summary derived from the register; `progress-checklist.html` was rebuilt from the ground up to render model badges, an overall program-progress bar, the defect summary, and a collapsible Phases 0–18 Foundation section.

**Phase 19.P / 19.I / 19.J / 19.D (2026-07-22) — condensed; full narrative in `docs/COMPLETED-TASKS-ARCHIVE.md`.** 19.P re-verified the whole stack clean before continuing. 19.I/19.J closed the interaction inventory and remaining user journeys via 4 parallel Sonnet subagents (defects D-19-21/25/29/34 found and fixed; every change independently re-verified after two subagents made false 'still waiting' claims; 89/89 combined regression). 19.D executed the owner-authorized production cleanup (9 orphaned Storage objects, 7 orphaned catalog rows, the test-reader account; all anti-joins rechecked to zero; the `supabase storage rm` silent no-op CLI limitation recorded).

**Phase 19.X — closeout complete (2026-07-22).** The prioritized Phases 20–24 implementation order is recorded in the "Phases 19–24 Completion Program" section above (matching the revised plan's own priority order: 20 → 21 → 22 → 23 → 24, with Phase 20's sub-phases in dependency order search → terminology/filter-visibility → permanent delete → source-text attach → reprocess reliability → canonical identity/duplicate collapse → cross-surface consistency → gate); manual VoiceOver remains recorded as an explicit agent limitation (no macOS Accessibility API access); the tag `phase-19-complete` was created and pushed (points at `f30992f`); `docs/project-status.json` records Phase 19 complete with 24 register findings, 0 open, 0 P0. **Phase 20 (Library integrity, file lifecycle, processing reliability, backend repair) is now the current phase.**

## Phase 20 wave narratives (moved from PROJECT-LOG 2026-07-23)

Verbatim Phase 20 wave 1 / wave 2 / wave 3 narrative paragraphs, moved out of `docs/PROJECT-LOG.md`'s "Current Implementation Status" section on 2026-07-23 to keep that file under the ~150,000-character tooling limit.

**Phase 20 wave 1 — 20.1 + 20.3 + 20.5 complete locally (2026-07-22).** Executed as three parallel, model-assigned subagents (Sonnet for 20.1; Opus for 20.3/20.5) under coordinator review with disjoint file ownership, then independently verified over the combined tree (workspace typecheck/lint clean; 392 unit tests green; production build clean; focused Playwright 47/47 across library/trash/work-status/security/rag). **20.1** adds server-authoritative, URL-synced Library search (`GET /api/library?q=`) over title/authors/venue/year/DOI/ISBN/source type with diacritic/case normalization, aria-live result counts, a no-results state, and 12 new CI-safe E2E tests; translator/editor/alias search is documented as a schema gap rather than silently claimed. **20.3** replaces the fire-and-forget purge with a pure `@ice/deletion` state machine and a durable `deletion_cleanup` record (additive migration `0030`, applied **local + production**, both ledger 31 — production pulled forward from the 20.8 gate the same day, see below): jobs cancelled, Storage bytes deleted before DB rows, any Storage failure persisted as a retryable `storage_failed` state reported honestly (never "success" while bytes remain), idempotent repeats, a typed-title focus-contained confirmation dialog, and a read-only admin cleanup queue. **20.5** turns reprocess into a single idempotent command driven by a pure `planReprocess` decision core, with a 60s worker run heartbeat, an on-read `stalled` signal, recovery UI, and orphaned-active-job cancellation replacing the manual runbook; the boot sweep now fails stuck documents with a visible reason, and CI's E2E tier gains `ANALYSIS_PIPELINE=v2`. Eleven register findings fixed (D-20-10, D-20-30..34, D-20-50..54 — 7 P1s, incl. RAG answering from trashed works, purge claiming success on Storage failure, duplicate paid reprocess runs, and two would-be P1s self-caught pre-commit). Commits `c728aa2`/`527979f`/`967db68`. Deferred to the 20.8 gate: the reprocess production canary and a durable `documents.confirmed_at` column (exact DDL recorded in the register/D-20-52). **Production migration 0030 was NOT deferred after all:** pushing wave 1 auto-deployed web/worker code that queries `deletion_cleanup`, so leaving production at ledger 30 would have 42P01-broken the trash/purge/admin surfaces until the gate — a deploy-ordering gap the coordinator caught immediately post-push and closed under the standing owner authorization via `supabase db query --linked` (additive-only payload + drizzle bookkeeping row; verified: production ledger 31, table present with 12 columns). Lesson recorded: with GitHub↔Vercel/Render auto-deploy on `main`, any commit whose code reads a new table must land its additive migration in production before or with the push, not at a later gate.

**Phase 20 wave 2 + cross-phase pull-forwards (2026-07-22).** 20.2 (terminology: every user-facing "Your work" → "Uploaded work(s)", exact-label + filter-visibility tests, `4ec20e5`), 20.4 (source-text attach for metadata-only Library works via a new `/library/[resourceId]` detail page reusing the secure upload pipeline, `5fc9fa6`, D-20-40/41), and 20.6 (canonical work identity: precedence-chain service with 23 unit tests, migration `0031` applied **local + production ledger 32 before commit**, reversible transactional merges, display collapse across Library/graph/RAG, and the mandated dry-run report showing all 21 local would-merge groups are E2E debris with zero genuine duplicates — `839aa6c`, D-20-60..65) are complete. The wave-1 CI red was root-caused to TWO env-parity gaps (real-Storage calls vs CI's dummy `SUPABASE_URL`, honestly reported `storage_failed` by the new machine; plus `PHASE_18_RAG_ENABLED` unset in CI) and fixed by splitting CI-safe vs manual-only real-Storage trash specs (`ea8ffd1`, D-20-55..59). Pull-forwards landed the same cycle: the fully-scripted signup-through-email-verification E2E closing the Phase 19.J residual gap (`e66d01e`, 10/10 across six runs; new open P3 D-19-35 — token tables lack a user FK), the Phase 23.1 internal link/anchor checker tool (`9db12eb`, zero broken links provisionally), and read-only audit dossiers for Phases 21 and 22 (`8740317`; D-21-1..10 and D-22-1..10 transcribed to the register as open-scheduled). One agent returned a placeholder report over genuinely sound work — caught and independently re-verified rather than trusted either way. Combined-tree verification before commits: 473 unit/integration tests, build clean, 81/82 focused E2E (the 1 failure being a stale Phase 19.J assertion, in repair). **Per the owner's 2026-07-22 continuous cross-phase parallelism directive, phases now run concurrently wherever conflict-free:** wave 3 is in flight with 20.7 (consistency, Sonnet), 21.1 (typed graph contract, Opus, folding D-21-1/10), D-21-6 (duplicate-edge fix, Sonnet), 22.1 (shared primitives with Docker pixel-stability proof, Opus), and 22.5 (global RAG sidebar, Sonnet, folding D-22-8/9). Also completed: owner-requested test-account cleanup — 77 local test users (incl. `owner-test@palimnote.local` and 76 stale E2E leftovers), 129 works/23 documents, and all 23 path-scoped Storage objects deleted and re-verified gone, with active concurrent test users correctly spared.

**Wave 3 + wind-down (2026-07-22).** The concurrent cross-phase wave completed and was independently verified over the combined tree (15-project typecheck, 400+ package tests, worker 76/76, `@ice/consistency` 62/62, production build, 104/104 focused E2E across 11 specs, zero reruns): **20.7** `@ice/consistency` — 9 deterministic cross-surface checks with a transactional, idempotent, never-guess repair applier and a report/--repair maintenance script; local report run found 0 critical/0 warning and 55 info-level `work_identity.year` backfills, repair-ready but deliberately unapplied (D-20-80, gate work) — `3ba321e`. **21.1** shared typed graph contract with edge-level relation filtering (D-21-1) and default-visible uploaded-work anchors (D-21-10), graph.spec.ts 14→18 — `3b8374d`; **D-21-6** classification-edge supersession on reprocess, red-then-green proven by stash-reverting the fix — `74f4aa2`. **22.1** shared annotation/roadmap/typography primitives with landing pixel-stability proven 3× through the official Playwright Docker image and `landing-contract.spec.ts` promoted to a running CI gate — `7b34c57`. **22.5** global Ask Library sidebar on every authenticated route (D-22-8/9 fixed to the established dialog standard; D-22-20 recorded open P3) — `546820e`. `baseline-test/` (the owner's new Roochnik gold-baseline `ARISTOTLE-ROOCHNIK-VICIOUS-001`, containing a copyrighted PDF) is gitignored per the Irwin-fixture discipline — `b00d8f7`. **Owner directives recorded for the continuing program (2026-07-22):** no user-facing "AI" language anywhere — explain each inference's basis instead (display-language change, provenance discipline unchanged); progressive disclosure site-wide (expanding boxes, no text flooding); all filters verified control→state→output across the site; phases amended where these aren't covered; the baseline test is a minimum correctness floor, never an exhaustive ceiling (the product legitimately finds more, across scholarly/popular/web sources); and the incoming coordinator (Fable 5) acts as moderator ONLY, delegating every task — including git, checklist, and log updates — to sub-agents on the cheapest fitting model. Per the owner's stop instruction, no further waves were launched from this session; Phase 20 stands at 7/8 (only the 20.8 gate remains), with the handoff prompt delivered out-of-band.
